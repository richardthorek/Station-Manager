# Backend API URL
VITE_API_URL=http://localhost:3000/api

# WebSocket URL
VITE_SOCKET_URL=http://localhost:3000

# StationKit suite — sibling app URLs (optional)
# The logged-in app launcher links out to sibling apps (separate deployments in
# suite Phase 1). Sensible production defaults are baked in; override for local
# dev or alternate domains.
# VITE_SANTA_RUN_URL=https://santa.stationkit.com.au
# VITE_FIREBREAK_URL=https://firebreak.stationkit.com.au
