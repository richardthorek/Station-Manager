# Server Configuration
PORT=3000
NODE_ENV=development

# Frontend URL for CORS
# For local development:
FRONTEND_URL=http://localhost:5173

# For Azure Static Web App deployment:
# FRONTEND_URL=https://<your-static-web-app-name>.azurestaticapps.net

# Multiple origins are supported automatically:
# - http://localhost:5173 (Vite dev)
# - http://localhost:4280 (Azure Static Web Apps CLI)
# - http://localhost:3000 (Backend serving frontend)
# - FRONTEND_URL value (production Static Web App)

# MongoDB Configuration
# For local development, use MongoDB connection string
# For Azure, use Azure Cosmos DB (Document DB) with MongoDB API connection string
MONGODB_URI=mongodb://localhost:27017/StationManager

# Azure Cosmos DB / Document DB Example (MongoDB API):
# MONGODB_URI=mongodb://your-cosmos-account:your-key@your-cosmos-account.mongo.cosmos.azure.com:10255/?ssl=true&replicaSet=globaldb&retrywrites=false&maxIdleTimeMS=120000&appName=@your-cosmos-account@

# To get your Azure Cosmos DB connection string:
# az cosmosdb keys list --name <your-cosmos-db-name> --resource-group <your-resource-group> --type connection-strings --query "connectionStrings[0].connectionString" --output tsv

# Azure Storage Configuration (for photo uploads)
# Required for truck check photo uploads (reference photos and result photos)
# AZURE_STORAGE_CONNECTION_STRING=DefaultEndpointsProtocol=https;AccountName=your-account;AccountKey=your-key;EndpointSuffix=core.windows.net

# Optional: Custom container names (defaults: reference-photos, result-photos)
# AZURE_STORAGE_REFERENCE_CONTAINER=reference-photos
# AZURE_STORAGE_RESULT_CONTAINER=result-photos

# To get your Azure Storage connection string:
# az storage account show-connection-string --name <your-storage-account-name> --resource-group <your-resource-group> --query "connectionString" --output tsv
