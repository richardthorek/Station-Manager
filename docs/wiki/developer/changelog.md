# Changelog — shipped work history

The dated record of what shipped, when, and why. New entries go at the **top of
the matching month section**; add a new month section as needed. The
forward-looking plan lives in [`docs/MASTER_PLAN.md`](../../MASTER_PLAN.md) —
this file is where its per-session shipping narratives land.

Entry format:

```
- 2026-MM-DD: **Short title (#PR).** One-sentence summary of the problem solved.
  **What shipped:** concrete changes. *N tests; suites green.*
```

Entries below were extracted verbatim from the pre-restructure master plan
(v4.x, July 2026) when the plan was rewritten as forward-intent-only. The full
old plan is preserved at
[`history/MASTER_PLAN_v4_2026-07.md`](history/MASTER_PLAN_v4_2026-07.md).
Doc paths quoted inside pre-restructure entries (e.g. `docs/current_state/…`,
`docs/archive/…`, `docs/api_register.json`) refer to the old layout; that
content now lives under `docs/wiki/developer/history/` and `docs/registers/`.

---

### July 2026 Stabilization

- 2026-07-17: **Q36 — gate `GET /api/achievements/:memberId*` behind `requireSession` (found investigating Q9).** It had no auth gate at all — unlike `members`/`checkins`/`events`/`reports`/`export`, which all got `requireSession({readsOnly:true})` in earlier F1/AC-4 fixes — so achievement/streak data (a real attendance-pattern PII signal, the same class those fixes closed) was reachable with zero credentials. **What shipped:** added `requireSession({readsOnly:true})` to the `/api/achievements` mount in `index.ts`, matching the `members` mount's treatment (no `requireFeature` — achievements are core member-profile data, not a separately-gated plan feature). Extended `anonymousAccessGate.test.ts` (the real-mount-chain suite that replicates index.ts, not just the bare router) to cover achievements alongside the other 6 gated mounts. Verified live: anonymous `GET /api/achievements/:id` now 401s; an authenticated request resolves normally (404 for a nonexistent member, not a leak). `api_register.json` updated. *Backend: tsc clean; 71 suites / 1079 tests green (1 new test group). No frontend changes — `UserProfilePage`'s existing authenticated call is unaffected.*
- 2026-07-17: **Restructured `MASTER_PLAN.md`: cut it back to intent + actionable next steps.** The plan had drifted from its own "forward-looking only" rule — the last-updated line had grown into a multi-paragraph shipping narrative duplicating this changelog, and done items carried full forensic writeups instead of just a status. Cut from ~50KB to ~16KB with no loss of traceability: every `Q`/`F`/`AC`/`D` id for still-open work preserved, closed-out blocks (L1, L2) collapsed to a one-line summary + pointer here, and the "Next steps" section now carries the actual actionable detail for what's left. *Docs-only change; no code/test impact.*
- 2026-07-17: **Q20 — split the 1.5 MB `vendor-export` chunk per format; found the real bug was worse than described.** The queue item said "a CSV export downloads the PDF/Excel engines too" — investigating found the actual impact was bigger: `papaparse` (CSV *import*, used only by `BulkImportModal` on the sign-in book) was grouped into the same `vendor-export` chunk as `exceljs`/`jspdf`/`html2canvas` purely by name coincidence, and `BulkImportModal` was a plain eager import in `SignInPage.tsx` — so simply landing on `/signin`, the single most-visited page in the app, downloaded the full 1.5 MB bundle (PDF/Excel/canvas rendering libraries) regardless of whether the visitor ever exported or bulk-imported anything.
  **What shipped:** split `exportUtils.ts` — previously one file with all three formats' static imports, so `exportUtils.lazy.ts`'s single shared dynamic-import target meant even *choosing* Excel pulled in jsPDF/html2canvas too — into `exportUtils.pdf.ts` (jsPDF + html2canvas), `exportUtils.excel.ts` (ExcelJS only), and `exportUtils.png.ts` (html2canvas only), each dynamically imported independently from `exportUtils.lazy.ts`. `vite.config.ts`'s `manualChunks` now gives each vendor library its own chunk (`vendor-export-pdf`, `vendor-export-excel`, `vendor-canvas` shared by PDF/PNG, and `vendor-csv-import` for papaparse — now correctly separated from export) with the SW `globIgnores` updated to match. Also made `BulkImportModal` `React.lazy()` (previously the only remaining eager import pulling in `vendor-csv-import` on every `/signin` load) — bulk CSV import now only loads when actually opened.
  **Verified in the built output** (not just unit tests, given this changes chunk topology): `exportUtils.excel-*.js` statically imports only `vendor-export-excel`; `exportUtils.png-*.js` only `vendor-canvas`; `SignInPage-*.js` reaches `BulkImportModal`/`vendor-csv-import` only through a genuine `import()` (`React.lazy`) call, not an eager static import. Combined output size of the four split chunks (~1549 KB) matches the original combined `vendor-export` size, confirming no functionality was lost — only regrouped. *Frontend: tsc + lint clean; 57 suites / 600 tests green. No logic changes (verbatim code move) — verified via build-output inspection rather than a full authenticated UI click-through, given the mechanical nature of the change.*
- 2026-07-17: **Q25 — Table Storage: fixed the hardcoded 3-month event window (found 2026-07-14).** `getEvents()` (and therefore `getEventsWithParticipants()`, which delegates to it) only ever scanned the last 3 calendar months regardless of `limit`/`offset` — deep pagination on the events list, and the org data export (Q26), silently returned nothing for any event older than 3 months in production. (`getEventsByDateRange`, used by Reports, was already fixed separately.) **What shipped:** `getEvents()` now pages backward through month partitions, newest first, until it has collected `offset + limit` matching events — since months are scanned strictly newest-to-oldest, once that count is reached after a full month's scan, every unscanned older month can only contain events older than everything already collected, so the requested page is fully determined; bounded to 120 months (10 years) so a deployment with no history at all doesn't scan forever. 4 new tests in a new `tableStorageEvents.test.ts` (using an in-memory fake of `@azure/data-tables`, one map per table name to avoid cross-table collisions) — confirmed failing against the old 3-month scan (an event 8 months old was invisible) before the fix, passing after. *Backend: tsc clean; 71 suites / 1075 tests green.*
- 2026-07-17: **Q9 — found + fixed two severe pre-existing access-control bugs on `/profile/:memberId` while chasing a "slow settle" report.** The literal ask was minor UAT polish (double SW-registration log; profile page settles in 3.5–5s). The SW log root-caused to a one-time Vite dev-server cold-start artifact — no fix needed. The "slow settle" investigation timed the page's four API calls directly (~4–6ms each with a valid credential, ruling out backend latency) and surfaced the real cause: two access-control bugs that made the page either 404 or 401 depending on how it was reached, presenting as a stuck spinner before failing.
  **Bug 1 — anonymous access fully broken.** `/profile/:memberId` is the intended "scan your own QR code" destination, deliberately left ungated in `App.tsx`, but `GET /api/members/:id`/`/history` require a session (pre-dates this session, from the 2026-06-22 UAT sweep) — any genuinely credential-less visitor got a broken error page, never a profile. Took an owner decision on intended behaviour: a personal QR scan should only ever offer sign-in (join an existing org session, or start one) — never broad, un-authenticated profile access — matching the product principle "individuals can join individual events but not get broad access," and consistent with the already-existing `/sign-in` personal-link check-in flow. **Fixed:** wrapped `/profile/:memberId` in `<AccessRoute>`, the same gate already used by `/signin` and `/truckcheck*` — a credential-less visitor is now sent to the front door instead of a broken error page. The backend 401 itself was already correct; the frontend just never respected it.
  **Bug 2 — station-scoping 404s any authenticated caller, independent of bug 1.** `GET/PUT/DELETE /api/members/:id`, `/:id/history`, and `/:id/invite` all required the caller's *currently-selected* `X-Station-Id` to match the target member's own station — so even a fully authenticated admin got "Member not found" for any member on a station other than whichever one happened to be selected client-side, including a fresh deep link with no prior station context at all (exactly what a direct profile link is). Took an owner decision here too: member ids are already globally unique UUIDs, so a valid org JWT should be able to look a member up directly by id regardless of the client's currently-selected station — the station match was never a real security boundary for JWT callers, just an accidental side effect of a client that hadn't happened to set `X-Station-Id` to match. **Fixed:** `flexibleAuth.ts`'s `AuthResult`/`verifyJwtAuthResult` now decode and carry the JWT's `organizationId`. `members.ts` gained `isMemberAccessible()`, applied across all 5 call sites (`GET /:id`, `GET /:id/history`, `PUT /:id`, `DELETE /:id`, `POST /:id/invite`) — falls back from the strict station match to an org match (the member's station's `organizationId` equals the caller's JWT `organizationId`) when they differ. Deliberately reads the raw `Authorization` header directly rather than relying on `req.auth` (populated inconsistently by `requireSession`/`flexibleAuth`, both of which are conditionally skipped depending on `ENABLE_DATA_PROTECTION`/HTTP method) — granting broader access to a cryptographically *verified* credential is safe to evaluate unconditionally, since it can never widen what an anonymous caller can do. Kiosk/brigade-token, member-session, and anonymous/demo callers are untouched — they carry no org identity, so they stay strictly station-scoped exactly as before.
  **Found, logged, deliberately not fixed here (same class of decision, out of scope for this pass):** `GET /api/achievements/:memberId*` has no auth gate at all — unlike members/checkins/events/reports, it's reachable with zero credentials today. Achievement/streak data reveals attendance patterns, the same PII class the 2026-06-22 UAT sweep and AC-4 closed elsewhere, so it likely belongs behind the same `requireSession` treatment — not touched here since it wasn't part of either owner decision above. *Backend: tsc clean; 70 suites / 1071 tests green (3 new tests in `members.test.ts` covering the cross-station-same-org allow case, the cross-org deny case, and the no-organizationId-in-token deny case; `authHelpers.ts`'s `createTestToken`/`authHeader` gained an optional `organizationId` param, backward compatible). Frontend: tsc + lint clean; 57 suites / 600 tests green. Verified live end-to-end against a fresh org/station/member (no test-mock shortcuts): an authenticated deep link with zero prior station context now renders the real profile instead of 404ing; a fully anonymous request is redirected to the front door instead of erroring.*
- 2026-07-17: **Q18 — strip debug `console.log` from production bundles.** ~31 `console.log` call sites shipped into the frontend production bundle (kiosk browser consoles filled with socket-join/offline-queue chatter, and any curious visitor could read internal debug detail). Vite 8 is Rolldown-based, so the old esbuild `pure` option doesn't apply; checked `build.rolldownOptions.output.minify.compress.dropConsole` first but it's all-or-nothing — drops `console.warn`/`console.error` too, which the plan explicitly wants kept for real production error visibility. **What shipped:** new `frontend/src/utils/debugLog.ts` — `debugLog(...args)` gated on `import.meta.env.DEV`, which Vite statically inlines to `false` and dead-code-eliminates in production builds. Converted all 31 sites across 8 files (`hooks/useSocket.ts`; `services/offlineQueue.ts`, `offlineStorage.ts`, `offlineSupport.ts`; `components/OfflineIndicator.tsx`, `InstallPrompt.tsx`, `AddToHomeScreenButton.tsx`; `main.tsx`) from `console.log` to `debugLog`. Verified against the actual built `dist/assets/*.js`: none of the 31 original log message strings appear anywhere in the production bundle; `console.warn`/`console.error` call sites are untouched and still present. *Frontend: tsc + lint clean; 57 suites / 600 tests green (no test changes needed — behavioural no-op in dev, log-only change in prod).*
- 2026-07-17: **Launch runway Block L2 started: F9 hygiene + F8 npm audit fixes.** Block L1 (Secure & private by default) merged (#665); starting Block L2 (Rock solid & fast). **F9 — hygiene:** removed the dead `web.config` (IIS/Windows config shipped into the Linux App Service deploy zip — ignored at runtime, and its `webSocket enabled="false"` would break Socket.io if the app were ever run on Windows) and the `cp web.config deploy/` packaging step in `ci-cd.yml`; fixed `ci-cd.yml`'s stale "Windows/IIS" header framing (deploy target is Linux) and its "smoke tests temporarily disabled" note (they run — see the file's own "RE-ENABLED" section further down, added a prior session and never reflected in the summary); fixed `architecture.md`'s "prod is still Windows/IIS, Linux migration is a tracked follow-up" warning, accurate in June 2026 but stale since prod (`bungrfs-linux`) migrated. **F8 — npm audit:** ran the non-breaking `npm audit fix` on both apps — backend 32 → 12 vulnerabilities (fixed `js-yaml`, `protobufjs`, several `@opentelemetry/*` packages), frontend 1 low → 0 (`@babel/core`). Remaining 12 backend moderates are all `@opentelemetry/*` transitively via Application Insights and need a breaking major-version bump — left for Dependabot per the review's own recommendation. *Backend: tsc + build clean, 70 suites / 1068 tests green. Frontend: tsc + lint + build clean, 57 suites / 600 tests green. No test/logic changes — dependency and doc/config hygiene only.*
- 2026-07-17: **Q33 — fix entitlement count limits counting globally instead of per-org; new Q35 ops follow-up.** `middleware/entitlements.ts`'s `enforceMemberLimit`/`enforceVehicleLimit`/`enforceStationLimit` called `db.getAllMembers()`/`db.getAllAppliances()` with no arguments and `db.getAllStations()` filtered only to exclude the demo/default station ids — none scoped the count to the calling organisation. Reproduced live: a brand-new Community-plan org (`maxVehicles: 1`) was refused its *first* appliance with `403 … current: 5` — the demo station's own seeded fleet alone exceeded the limit, so every Community org's vehicle quota was effectively shared platform-wide. Root cause went one level deeper: **no station anywhere in the system ever had `organizationId` set** — the field existed on the `Station` type since the SaaS tenancy model shipped, but nothing ever wrote it (`POST /api/stations` never assigned it).
  **What shipped:** `POST /api/stations` now tags a newly-created station with `organizationId: req.organization?.id` (added `attachOrganization` to that route). `entitlements.ts` gained a `getOrgStationIds(organizationId)` helper resolving an org's own station ids; `enforceMemberLimit`/`enforceVehicleLimit` now filter `getAllMembers()`/`getAllAppliances()` to rows whose `stationId` is in that set before comparing to the plan limit, and `enforceStationLimit` filters `getAllStations()` by `organizationId` too (previously only excluded the built-in demo/default ids). Rewrote `entitlementLimits.test.ts` — the previous version mocked the count function directly and never exercised the real per-station logic, which is exactly how this shipped unnoticed; it now models real station-scoped fixtures and adds explicit cross-org-isolation tests (5 new). Verified live end-to-end against a real running server with entitlements **on** (no dev bypass): a fresh Community org's first vehicle now succeeds regardless of any other org's or the demo station's fleet size; a second vehicle on the *same* org still correctly 403s at `current: 1`; a fresh org's first station still succeeds regardless of other orgs' station counts.
  **Deliberately not solved here — new Q35:** any station created *before* this fix has no `organizationId` and cannot be reliably backfilled (no `createdBy`/audit trail links an existing station to the org that created it). The fallback fails open — an untagged station's members/vehicles count toward no org's limit, never falsely blocking a *different* org, the safe direction — but it also means a pre-existing production org's limit won't actually enforce against its own data until reconciled. Logged as a one-time, owner-reviewed ops task; an automatic guess risks mis-attributing a station to the wrong org, worse than the current under-enforcement. *Backend: tsc clean; 70 suites / 1068 tests green (6 new tests). Manually verified against a live dev server (entitlements enabled, no bypass): reproduced the original bug on a fresh checkout, then confirmed it's gone after the fix, including the cross-org and same-org-still-enforces cases.*
- 2026-07-17: **Q30 — process-level crash handlers, closing out Block L1 in code.** No `process.on('unhandledRejection'|'uncaughtException')` existed, so a single un-awaited rejection in an async socket/route handler would crash the one App Service instance with no logged reason and no telemetry flush — a full outage with a silent cause. **What shipped:** `services/fatalErrorHandler.ts` exports `handleFatalProcessError()` (log via `logger.error`, flush App Insights, then `process.exit(1)` so the platform restarts a known-good process rather than continuing in an undefined state — exit 1 to distinguish a crash from SIGTERM/SIGINT's existing graceful exit 0); wired via `process.on('uncaughtException'|'unhandledRejection')` in `index.ts`. Extracted into its own module (rather than inlined, since `index.ts` self-executes on import and isn't otherwise unit-testable) so the log/flush/exit behaviour is directly testable with injected fakes instead of needing to trigger a real process fault. Verified live: built + ran the compiled server standalone, confirmed normal startup/shutdown is unaffected by the new listeners. **Deliberately not done here:** the paired "run ≥2 instances" recommendation — the current App Service Plan is Basic (B1), which supports manual scale-out up to 3 instances without a tier upgrade, but it's still a real compute-cost increase; left as an owner spend decision (same category as D6), not something to change unilaterally in a security-focused PR. With this, **every code-level item in the launch runway's Block L1 (Secure & private by default) is done** — the only things left in L1 are two owner decisions (`REQUIRE_AUTH=true` prod verification, Q30's instance count) that need Azure access/spend authority this session doesn't have. *Backend: tsc clean; 70 suites / 1062 tests green (3 new tests). Manually verified: built server starts and responds normally with the handlers registered; no `MaxListenersExceededWarning` across the full Jest suite (auto-mocked `../index` imports don't leak listeners across test files).*
- 2026-07-17: **AC-4 — walk-up consistency sweep: found + closed a real anonymous member-PII leak on activities/checkins/events; closed F1's test-coverage follow-up; found + fixed a live demo-mode bug.** Audited every `/api/*` mount in `index.ts` against its *actual* internal auth (not just `requireFeature`, which never blocks anonymous callers on its own — that was the whole lesson of F1). Found `activities.ts`/`checkins.ts`/`events.ts` relied solely on `flexibleAuth`, which — exactly like `REQUIRE_AUTH` (Q29) — only enforces auth when the `ENABLE_DATA_PROTECTION` env var is explicitly `true`; unlike `members`/`truck-checks`/`reports`/`export` (already fixed by the 2026-06-22 UAT sweep and F1), these three had no unconditional `requireSession` backstop. `checkins`/`events` return real `memberName`/`memberRank`/check-in timestamps/`location` (`EventParticipant`/`CheckIn`) — the same class of anonymous PII leak F1 closed elsewhere, just not caught by that sweep. **What shipped:** added `requireSession({readsOnly:true})` to all three mounts (writes keep their existing kiosk pass-through, matching the established pattern); confirmed `organizations`/`billing`/`platform`/`agent-sessions` were already unconditionally gated and `stations`/`facilities`/`org-invites` are intentionally public (signup/kiosk-selection flows, low-sensitivity data) — not touched. Also closed the still-open F1 follow-up (flagged in that PR's own notes but never done): a new `anonymousAccessGate.test.ts` replicates the real mount chain — `requireSession`+`requireFeature` together, not just the bare router `export.test.ts`/friends mount — for all 7 now-gated routes (members, activities, checkins, events, truck-checks, reports, export), asserting 401 for a credential-less non-demo request and pass-through for JWT/brigade-token/demo.
  **Found and fixed along the way (surfaced by actually driving the demo flow in a browser while verifying the above):** `StationContext.tsx`'s "normal mode" branch explicitly nulled the current station id even when `isDemoActive()` was true, so an anonymous demo visitor's requests never carried `X-Station-Id: demo-station` — meaning `requireSession`'s demo bypass (which requires the request to explicitly target the demo station) never matched, and now that checkins/events are also gated, the *entire* demo sign-in book would have 401'd, not just the member roster. Root-caused one level deeper: the fix wanted to call the already-existing (but never wired up) `api.getDemoStation()`, which turned out to itself be broken — it called `GET /stations/demo`, but the route is `GET /stations/:id` and the real id is `demo-station`, so it always 404'd and had evidently never been exercised. Fixed both: `getDemoStation()` now hits the correct URL, and `StationContext` calls it to resolve `currentStationId`/`selectedStation` to the demo station for an anonymous demo visitor. Verified end-to-end in a browser: `/signin` and `/truckcheck` under `?demo=true` now correctly show their real (empty) demo-station state instead of 401ing.
  **Found, logged, deliberately not fixed (separate, larger scope):** `enforceMemberLimit`/`enforceVehicleLimit`/`enforceStationLimit` count members/vehicles/stations **globally across every organisation** instead of scoping to the caller's own org (`middleware/entitlements.ts` calls `db.getAllMembers()`/`getAllAppliances()` with no arguments, even though both already accept a station filter). Reproduced live: a brand-new Community-plan org (`maxVehicles: 1`) was refused its first appliance with "current: 5" — the demo station's own seeded fleet alone exceeds the limit. Logged as Q33 — likely already blocking real Community-tier signups from adding a vehicle/member in production; a billing/entitlement correctness fix, not an access/security one, so out of scope here. *Backend: tsc clean; 69 suites / 1059 tests green (29 new tests: 28 in `anonymousAccessGate.test.ts` + 1 in a new `corsHeaders.test.ts` from the AC-3 commit). Frontend: tsc + lint clean; 57 suites / 600 tests green (2 new tests). Manually verified in Chromium (headless, via Playwright) against a live dev backend + frontend.*
- 2026-07-17: **AC-3 — truck-check share/join model + gate `/truckcheck`; found + fixed a CORS gap; found + logged two separate bugs (Q33, Q34).** Closed the last unstarted piece of the access & membership model (owner, 2026-07-04): `/truckcheck*` had no `AccessRoute` gate, so it was reachable by a bare URL with no credential — the one remaining gap after `/signin` got the same treatment (AC-1/AC-2/AC-5). The "start a check" half of the join model already existed (`POST /runs` auto-joins an in-progress run for the same appliance — multi-contributor checks were already built); what was missing was a legitimate way to *get* a teammate onto that page without a full admin login. **What shipped:** a "Share this check" action on the active check workflow (`CheckWorkflowPage`) opens a modal (`TruckCheckShareModal`, QR + copyable link) that reuses the **existing** brigade/device-token system exactly as a kiosk sign-in link does — if the device is already in kiosk mode it reuses its own token (`generateKioskUrl()`); otherwise it mints a fresh 24h station-scoped token via the now-admin-gated `POST /api/brigade-access/generate` (Q29) — landing on `/truckcheck/check/:applianceId?brigade=<token>`, which `AccessRoute`'s existing `isKioskMode()` check already recognises. No new credential type needed. All 9 `/truckcheck*` routes now wrapped in `AccessRoute`, same discipline as `/signin`.
  **Verified end-to-end in a real browser** (not just unit tests): bare `/truckcheck` redirects to the marketing home; an admin creates a vehicle + check, clicks Share, and a **second, fully separate, logged-out browser context** opens the generated link and lands correctly on that vehicle's check page. Along the way this surfaced a real cross-origin CORS bug — the Express `cors()` config's `allowedHeaders` never included `X-Brigade-Token` or `X-Member-Session` (only `Authorization`/`X-Station-Id`), even though `api.ts` has sent both for a while. Invisible in prod today (frontend and backend share one origin, so no preflight fires) but would silently break any cross-origin caller using a kiosk or member-session credential — exactly the shape of request a separately-hosted Bushie Tools suite app would make. **Fixed**: `allowedHeaders` now includes both, extracted to a small testable `config/corsHeaders.ts` module.
  **Also found, logged, deliberately not fixed here (both out of scope for this PR):** (1) **Q33** — `enforceMemberLimit`/`enforceVehicleLimit`/`enforceStationLimit` (`middleware/entitlements.ts`) count members/vehicles/stations **globally** across every organisation instead of scoping to the caller's own org, even though the underlying DB methods already accept a station filter that just isn't passed. Reproduced live: a brand-new Community-plan org (`maxVehicles: 1`) was refused its *first* appliance with "current: 5" — the demo station's own seeded fleet alone exceeds the limit. This likely already blocks real Community-tier signups from adding a vehicle/member in production. (2) **Q34** — demo mode (`?demo=true`) never sets a station id outside kiosk mode (`StationContext.tsx` explicitly nulls it in "normal mode"), so `requireSession`'s demo bypass — which requires the request to explicitly target `demo-station` — never matches, and every `requireSession`-gated read 401s for an anonymous demo visitor (reproduced on both `/signin`'s member roster and `/truckcheck`'s appliance list; the former degrades gracefully to an empty state, the latter shows a harder "Failed to load vehicles"). *Backend: tsc clean; 68 suites / 1031 tests green (5 new tests). Frontend: tsc + lint clean; 57 suites / 598 tests green (6 new tests). Manually verified in Chromium (headless, via Playwright) against a live dev backend + frontend, including a genuine cross-context share-link join.*
- 2026-07-17: **Launch runway L1 — Secure & private by default: brigade-access + rollover auth, AI gateway rate limit, Socket.io credential gating (Q29 code fix, F6, Q31, F7).** Working through the launch runway's Block L1 in one PR. Per the 2026-07-17 security review: (F5) `POST /api/brigade-access/generate`, `GET /api/brigade-access/all-tokens`, `/stats`, and their sibling admin endpoints (`DELETE /:token`, `GET /brigade/:brigadeId`, `GET /station/:stationId`) used `optionalAuth`, which only enforces authentication when the `REQUIRE_AUTH` App Service setting is `true` — if unset in prod, any caller could mint a kiosk credential for any station or dump every active kiosk token in one request; (F6) `POST /api/events/admin/rollover` was a fully unauthenticated state-changing admin route; (F4) `/api/ai/chat`, `/report`, and `/speech/token` have no auth and pass the AI gate when there's no org context, so an anonymous caller could drive the server-side Azure OpenAI/Speech gateway bounded only by the generous general-purpose `apiRateLimiter` (~84 req/5 min); (F7) the Socket.io `join-station` handler accepted any client-claimed `stationId` with **no credential at all**, so a socket that merely knew a station id could join its room and inject spoofed `checkin`/`activity-change`/`member-added`/`event-created`/`event-ended`/`participant-change` events onto that station's kiosks, with no payload validation either.
  **What shipped:** brigade-access + rollover route groups now require `authMiddleware` + `requireAdmin` unconditionally (matching the established pattern in `devices.ts`/`truckChecks.ts`), removing the dependency on `REQUIRE_AUTH` entirely for these admin surfaces; `POST /api/brigade-access/validate` stays public (the kiosk itself calls it from `/signin?brigade=<token>`). A new dedicated `aiRateLimiter` (30 req/5 min per IP, `middleware/rateLimiter.ts`) now sits on the whole `/api/ai` mount in place of the general limiter — deliberately **not** requiring a session, since AAR Studio's signed-out, local-first use is an intentional product feature (A2), not a gap; the review offered the tighter-limiter path as an alternative to auth for exactly this reason. `join-station` now requires the same credential model `requireSession`/`flexibleAuth` already enforce on the equivalent REST reads — an admin JWT (any role, any station), or a brigade/kiosk or member-session token scoped to the one station it was minted for — with the public demo station staying open; `flexibleAuth.ts` gained pure token-based verification functions (`verifyJwtAuthResult`/`resolveBrigadeTokenAuthResult`/`verifyMemberSessionAuthResult`) shared between the REST middleware and the new Socket.io check. The six broadcast events now reject any payload that isn't a plain object under a 32KB cap before rebroadcasting. The whole station-socket handler set was extracted from its inline block in `index.ts` into `services/stationSocketHandlers.ts` (mirroring the existing `aarCollab.ts` pattern) so it's unit-testable with a mock socket instead of only reachable via a live server. `frontend/src/hooks/useSocket.ts` now sends whichever credential the device holds (`auth_token`/kiosk token/member-session token) as extra `join-station` fields. Added 401-for-anonymous-caller tests to the brigade-access and rollover route test files (mirroring the regression-test gap the F1 fix flagged), a rate-limit-header test for the new AI limiter, and a new 24-test `stationSocketHandlers.test.ts` suite covering the credential matrix and payload validation.
  **Not done in this pass:** confirming the `REQUIRE_AUTH=true` App Service setting in prod (needs Azure portal/CLI access not available in this session — the code fix above makes it moot for the brigade-access routes specifically, but other `optionalAuth` usage, e.g. `stations.ts`'s intentionally-public national-dataset lookups, still depends on it); Q30 (process-level crash handlers) remains the one open L1 item besides AC-3/AC-4. *Backend: tsc clean; 67 suites / 1030 tests green (30 new tests). Frontend: tsc + lint clean; 57 suites / 593 tests green (1 new test).*
- 2026-07-17: **Plan: platform-owner console spec'd + queue restructured into a launch runway.** Per owner: a platform-owner (SaaS operator) super-admin account is a launch requirement — cross-org visibility of users/organisations/subscriptions + aggregate usage stats, plus management of account *structure* (org membership & roles, plan/entitlements, org status, and destructive delete of orgs/users), all behind a hard privacy wall (the operator can never read tenant operational data — member PII, sign-ins, truck checks, AAR content — only account metadata + derived aggregate counts, Stripe-dashboard style). **What changed (planning only, no implementation):** added feature-board row #19 and queue item Q32 with the full spec + enforcement approach (dedicated `requirePlatformAdmin` `/api/platform` endpoints that structurally cannot return row-level tenant records); and **restructured `MASTER_PLAN.md`'s queue into a "launch runway"** — three launch blocks (L1 Secure & private by default, L2 Rock solid & fast, L3 Run the business) that contain all remaining launch work, plus a post-launch block — grouping every existing Q/F/AC item under the launch bar it serves (rock-solid, secure, performant, reliable). Security-review findings F6 (unauth rollover) and F7 (unauth/unvalidated sockets) promoted into L1; D6 staging-slot zero-downtime deploy pulled into L2. *Docs only.*
- 2026-07-17: **Pre-launch stability/security review + close anonymous `/api/export` data leak.** A focused stability/security/safety review ahead of launch ([PRE_LAUNCH_STABILITY_SECURITY_REVIEW_20260717](history/reviews/PRE_LAUNCH_STABILITY_SECURITY_REVIEW_20260717.md)) found that the `/api/export/*` endpoints were mounted with `requireFeature('reportsEnabled')` but **without** `requireSession` — and `requireFeature` passes through when there's no org context, so a credential-less caller could `GET /api/export/members` and receive the full roster as CSV including each member's **QR check-in code** (an auth credential), for any station via the attacker-controlled `X-Station-Id` header. This was the exact class of hole the 2026-06-22 UAT fix closed on members/reports/truck-checks, but `/api/export` was missed in that sweep (and the export tests mount the router without the `index.ts` middleware, so they never caught it). **What shipped:** added `requireSession({ readsOnly: true })` to the `/api/export` mount in `index.ts`, mirroring the adjacent `/api/reports` line — admin JWTs and kiosk brigade tokens still work, demo stays open, anonymous is now 401. The review also documents F2–F9 (zero-downtime deploy = D6, missing process-level crash handlers, unauthenticated AI + brigade-access-admin routes, lazy/unauthenticated event rollover, unvalidated socket payloads, 30 moderate transitive npm advisories) as owner-triaged recommendations, not code changes. *Backend: tsc clean; export suite 15/15 green. Docs + one-line security fix; new queue items Q29–Q31 and Feature #15 flipped to 🟡.*
- 2026-07-16: **Voice-check WS: fix engine.io killing slow same-server upgrades (Q27) + live post-deploy probe.** Reproduced and closed the latent race the plan tracked as Q27: Socket.io's engine registers its own `'upgrade'` listener on the shared `httpServer`, and its default `destroyUpgrade` behaviour ends any upgrade socket it didn't handle after ~1 s with no bytes written — but `/ws/agent-check` awaits several Table Storage lookups (kiosk-token/appliance/station/org/resume-session) *before* writing the 101, so under prod latency the handshake died mid-validation ("socket hang up"); never reproducible locally against the instant in-memory DB, and invisible to the existing tests, which attach the handler to a bare server with no Socket.io. **What shipped:** `destroyUpgrade: false` on the Socket.io server (`index.ts`); the agent-check upgrade handler takes over engine.io's cleanup duty, immediately 404-ing upgrade paths that neither it nor Socket.io (`/socket.io/`) handles so nothing hangs open; a new "coexistence" test group attaches the WS handler and Socket.io to one server (prod shape) and proves a >1 s validation still completes, fast rejections still return HTTP statuses, and unknown upgrade paths get 404; and `postDeploymentTests.ts` gained a live probe that sends a real same-origin Upgrade request to `/ws/agent-check` expecting the handler's 401 — distinguishing every historical failure mode (200 = SPA-fallback regression, 403 = origin-gate regression, no response = destroyUpgrade regression) on every `main` deploy. Also confirmed against the live site that the #660 same-origin fix is deployed and answering correctly (the 05:05/10:05 deploys of #655/#657 had **failed** on the missing `staging` slot, so those fixes weren't live until 10:30+). *Backend: agentCheckWs suite 37/37; full suite 66 suites / 1000 tests green; tsc clean; post-deploy script verified end-to-end against a locally-run prod build.*
- 2026-07-16: **Voice Check still failed to connect on the live custom domain — same-origin `/ws/agent-check` upgrade hard-403'd (#660).** The earlier same-day fix cleared the kiosk-token **401**, but the voice agent WS kept failing with a bare "Connection error" on `bushietools.com`. Root cause was a second, independent gate: `agentCheck.ts`'s upgrade handler rejected any `Origin` not literally in `FRONTEND_URLS` with a 403 — **including same-origin requests** — running that check *before* auth. The rest of the app (Express CORS + Socket.io) only *withholds CORS headers* for unlisted origins (`callback(null, false)`), so a same-origin caller still succeeds; that asymmetry is exactly why `useSocket.ts` (WebSocket-only transport) logged "Socket connected" while the identical same-origin voice-check upgrade was refused whenever `FRONTEND_URLS` didn't list the serving domain (as after a custom-domain move). **What shipped:** `isUpgradeOriginAllowed()` now permits a request with no Origin, an allow-listed Origin, **or** a same-origin request (Origin host matches the `Host`/`X-Forwarded-Host` header) — mirroring the app-wide convention — while still rejecting genuine unlisted cross-origin upgrades (WebSocket-hijacking protection). Added a same-origin regression test (Origin host matches the request Host but is absent from `allowedOriginsList`). *Backend: agentCheckWs suite 34/34 green; tsc clean. Ops note: adding the live domain to `FRONTEND_URLS` is still recommended so cross-origin/CORS paths are correct, but voice check no longer depends on it.*
- 2026-07-16: **Fix three critical bugs: Voice Check connect error, landing-page top row clipped on tablet, QR device sign-in bounced home.** (1) Voice Check (`/truckcheck/voice/:applianceId`) is gated by `FeatureRoute` only, no `ProtectedRoute`, so a walk-up kiosk/tablet with no admin JWT could reach the page — but the `/ws/agent-check` upgrade handler unconditionally required a valid JWT, rejecting the socket with 401 and surfacing as a bare "Connection error" to every non-admin visitor. (2) `LandingPage`'s app-picker grid used `align-items: flex-end` inside an `overflow: hidden` container; on tablet-width viewports (between the 768px mobile breakpoint and desktop) with enough cards to wrap to two rows, `flex-end` pushed the grid above the container's top edge and the browser's default scroll anchor (start-edge) made that clipped content unreachable — the page looked anchored to the bottom with the top row hidden under the header. (3) `LoginPage`'s in-app QR scanner validated the scanned token against `/api/devices/validate` and stored it under ad-hoc `deviceToken`/`stationId`/`deviceContext` localStorage keys that `isKioskMode()`/`AccessRoute` don't recognise, so a successful scan still bounced back to `/` instead of signing the device in.
  **What shipped:** `backend/src/routes/agentCheck.ts`'s WS upgrade now falls back to `resolveKioskAccess()` (the same brigade/device-token resolver `flexibleAuth`/`requireSession` already use) when the token isn't a valid JWT, and enforces the resolved station matches the appliance's station; `voiceAgentClient.ts` sends the kiosk token (`getKioskToken()`) when there's no admin `auth_token`. `LandingPage.css`: `.landing-page`/`.landing-main` now scroll (`overflow-y: auto` / `visible`) instead of clipping, and `.cards-area` uses `align-items: flex-start`. `LoginPage.tsx`'s `handleQRScan` now validates via `/api/brigade-access/validate` and hands off to `/signin?brigade=<token>`, the same activation path a printed kiosk QR already uses. *Backend: 66 suites / 996 tests green. Frontend: lint + typecheck clean, 56 suites / 592 tests green.*
- 2026-07-14: **Refresh link-preview og-image + purge stale manifest branding.** Shared links (messaging apps, social) still unfurled with the retired fire-station-building logo and old lime/house branding, because `og-image.png` was never regenerated after the design-system-v2.0 rebrand (#618), and `site.webmanifest` still carried the pre-rebrand "Station Manager" name with a black theme colour. **What shipped:** regenerated `frontend/public/og-image.png` (1200×630) on the v2.0 brand — Signal Flame mark on a Signal Red tile, cool-slate gradient, Hi-Vis Amber accent, Archivo/Public Sans, "Bushie Tools — Your crew toolkit" with the four-tool feature chips (rendered via headless Chromium with the brand fonts inlined); rebranded `site.webmanifest` to "Bushie Tools" with theme `#D8232A` / background `#ffffff` so a direct `/site.webmanifest` probe is consistent with `manifest.json`; and set `browserconfig.xml` Windows tile colour to `#D8232A` to match `index.html`. Also **regenerated the full raster icon set as full-bleed** — the previous apple-touch/android-chrome/favicon PNGs were rounded-corner tiles with transparent corners (and the apple-touch even had a transparent band along the bottom edge), so iOS/Android masking and any placement on a light surface let white poke through around the flame; the icons now fill the square edge-to-edge in Signal Red with the white Signal Flame centred in the maskable safe zone, so the OS rounds them cleanly. Rebuilt `apple-touch-icon*.png`, `android-chrome-{192,512}.png`, `ms-icon`/`mstile-*.png`, `favicon-{16,32,48,96}.png`, a fresh multi-size `favicon.ico`, and squared off the `icon.svg`/`favicon.svg` sources (`rx` removed). *Static-asset + manifest-only change; no code touched, so no test surface affected.*
- 2026-07-14: **Blind-spot sweep: JWT fail-fast, Table Storage backups, optimistic concurrency, org data export.** A project-review pass flagged five gaps not tracked anywhere: `JWT_SECRET` had a silently-forgeable dev fallback that would also run in production if the env var were ever missed; Table Storage has no native backup/restore and nothing exported it; every Table Storage read-modify-write was unconditional last-write-wins (no ETag), risking silently dropped edits from two devices touching the same row; there was no org data export mechanism for privacy/retention requests; and a fifth item, staging-slot pre-swap deploys, turned out to already have CI-side detection logic landing in parallel (#649) — see D6, no duplicate work done here.
  **What shipped:** (1) `backend/src/config/jwtSecret.ts` centralizes the secret (was duplicated across 7 files); the server now refuses to start in production if `JWT_SECRET` is unset or still the dev default. (2) `backend/src/scripts/backupTableStorage.ts` — discovers every table via `TableServiceClient.listTables()` and exports it to timestamped NDJSON blobs (`npm run backup:prod`); wired to a new nightly `nightly-backup.yml` workflow (needs the `AZURE_STORAGE_CONNECTION_STRING` repo secret set as a one-time ops step). (3) `tableStorageDatabase.ts` gained `updateEntityOptimistic()` — ETag-based optimistic concurrency with refetch-and-retry on a 412 — applied to `endEvent`/`reactivateEvent`/`deleteEvent` and `updateMember`/`updateMemberAuth`/`deleteMember`. While in there, fixed a real bug found along the way: `getEventsByDateRange` delegated to `getEvents()`, which only ever scans the last 3 calendar months regardless of the range requested — silently truncating `getAttendanceSummary`/`getMemberParticipation`/`getActivityBreakdown` (the Reports feature) for any wider range in production. It now scans the actual requested month partitions. (Note: `getEvents()`/`getEventsWithParticipants()` themselves are unchanged and still 3-month-capped — this affects the events-list UI's deep pagination and is tracked as Q25, a larger change deferred pending review.) (4) `GET /api/organizations/current/export` (owner-only) — downloads a JSON bundle of the org's stations, members, and event/attendance history, with an "Export organisation data" button on the Organization admin page. Known limitation, documented in the response itself: event history inherits the ~3-month cap above; truck-check history and device records aren't included yet (Q26). *Backend: 66 suites / 997 tests green (3 new tests). Frontend: lint + typecheck clean, 57 suites / 608 tests green (2 new tests). No live-Table-Storage integration test was possible in this sandbox — verified by typecheck + the in-memory test suite; the ETag/backup code paths should get a first real run against a dev storage account before being trusted blind in prod.*
- 2026-07-13: **Org onboarding: facility-claim signup, invite links, multi-org membership, claim-conflict review.** The Organizations table was empty in production — the `grant:firebreak` backfill had nothing to act on, users couldn't be assigned to orgs, and entitlements were never enforced. Signup created a free-text-named org with zero duplicate detection, users belonged to at most one org, and the only invite flow hardcoded the first org in the store (a real bug once more than one org exists).
  **What shipped:** (1) Signup now requires an `email` and a `facility` selection anchored to the Digital Atlas of Australia / Geoscience Australia "Emergency Management Facilities" dataset across **all** service types (rural/country fire, metro fire, SES, ambulance, police, other) — first-come-first-served claiming keyed on a composite `"<serviceType>:<objectid>"` facilityKey; a duplicate claim returns `409 FACILITY_ALREADY_CLAIMED` and writes a `ClaimConflict` for platform review, with a "my unit isn't listed" custom path (no admin review) that still nudges toward known facilities via live search. (2) `OrganizationMembership`/`OrgInvite`/`ClaimConflict` — one new DB triple (`orgAccessDatabase.ts` + Table Storage twin + factory) — make multi-org membership real: a user can belong to several orgs with a different role in each, switchable via `POST /api/auth/switch-org` (reissues the JWT; claim shape unchanged). Legacy single-org users migrate lazily, no manual backfill needed. (3) Org invite links (`/api/organizations/current/invites`, public `/api/org-invites/:token`) are multi-use, 7-day default expiry — shareable in a brigade group chat, unlike Fire Santa Run's single-use invites. Fixed the member-activation first-org bug along the way (`inviteOrganizationId` now recorded on the invite). (4) Platform admin (`PLATFORM_ADMIN_USERNAMES` allowlist, not an org role) reviews/resolves claim conflicts at `/admin/platform` — dismiss, mark contacted, or reassign the facility to a different org. (5) Frontend: two-step signup stepper (account details → `FacilitySearch`), `/invite/:token` acceptance page, an org switcher in the admin nav, and a Members/Invite-links section on the Organization page. `GET /api/auth/me` gained additive-only fields (`email`, `memberships`, `isPlatformAdmin`) — the Fire Break Calculator suite contract is unchanged. **Remaining ops:** run `npm run facilities:fetch` (needs internet access to `services.ga.gov.au` — from an operator machine, not CI) then `facilities:upload` to seed the facility snapshot; set `PLATFORM_ADMIN_USERNAMES`. Follow-ups queued in `docs/MASTER_PLAN.md`: reconcile facility claims with Fire Santa Run's brigade identity, converge `facilitiesParser.ts` with the existing `rfsFacilitiesParser.ts`, email delivery of invites. *Backend: 65 suites / 988 tests green (7 new suites, ~74 new tests). Frontend: lint + typecheck clean, 56 suites / 590 tests green (4 new suites).*
- 2026-07-13: **Fire Break Calculator suite-access wiring audit (#647, paired fireBreakCalculator #172).** A subscribed owner still saw the Fire Break Calculator as "Coming soon" (marketing page) and "Not in your plan" (launcher) after #638. Audit conclusion: the code contract is correct end to end (plans → `/api/auth/me` → launcher gate; FBC token validation) — the blockers are stale marketing copy, missing deploy-time config in the FBC repo, and two never-run ops steps.
  **What shipped:** marketing page Fire Break card is now a live card (badge removed) and the Basic plan card lists the calculator; `ci-cd.yml` passes optional `VITE_FIREBREAK_URL`/`VITE_SANTA_RUN_URL` repo variables into the frontend build. Paired FBC PR wires `VITE_SUITE_AUTH_URL` (webapp build) + `SUITE_AUTH_URL` (SWA app setting, via Bicep) into its deploy pipeline. **Outstanding ops (already queued on feature-board item 13):** run `npm run grant:firebreak` against prod (stored entitlement snapshots predate the #638 grant — this is why the launcher shows locked) and add the FBC origin to the App Service `FRONTEND_URLS`. *Frontend 569 tests, lint + typecheck green; no backend changes.*
- 2026-07-12: **Marketing showcase + pre-launch performance & polish sweep (#644).** Pre-launch push: the logged-out front door showed static cards where first-time visitors needed to *see* the product working, `/login` took 3.5–5s to settle, the PWA install weighed 8.6 MB, and the truck-check workflow + AAR capture looked a generation older than the rest of the app. **What shipped:** (1) Apple-style scroll showcase on the marketing page — four screen-recording-style animated demos in stylised device frames (one-tap sign-in with live sync feed, guided Cat 1 truck check flagging an issue, AAR Studio waveform + live transcript + AI Sustain/Improve/Action findings board, reports dashboard with count-up KPIs and volunteer-hours chart), scroll-revealed, viewport-gated timers, reduced-motion safe, CTA into pricing. (2) Performance: `QRScannerModal` (~370 KB html5-qrcode) lazy-loaded so the LoginPage chunk fell 376 KB → 4.3 KB (fixes the Q9 `/login` slow settle); heavyweight on-demand chunks excluded from the SW precache with a runtime CacheFirst fallback (install precache 8.6 MB → 1.4 MB); unused 4 MB `app-icons/` export moved out of `public/`; bundle-visualizer report no longer emitted into `dist/`; immutable Cache-Control on hashed `/assets` + no-cache on the SPA shell/sw.js; Clarity analytics deferred. (3) Truck-check workflow re-skinned onto design-system-v2 tokens: compact left-aligned item cards, status-coloured Done/Issue/Skip (fixing a specificity bug that rendered all three dark navy), token-surface sidebar, un-clipped header, no more empty photo-placeholder boxes. (4) AAR capture: the single level bar replaced with a 24-bar live waveform driven by the real mic RMS levels; **hotfix — smart-quote string delimiters introduced in #620 made `capture.js` fail to parse, breaking the entire AAR capture view in production**. (5) Domain: canonical/OG/Twitter URLs set to bushietools.com for launch. *Frontend 560 green (+7 showcase tests); backend 914 green; AAR 116 green; verified in Chromium at iPad portrait/landscape, light + dark.*
- 2026-07-12: **Truck check onboarding wizard (#639).** New users signing up now see a post-signup 4-step onboarding flow to seed their vehicle checklist library. **What shipped:** multi-step wizard (`TruckCheckOnboardingWizard.tsx`) collecting jurisdiction (state), agency/organisation with "other" free-text option, and multi-select vehicle types from 20+ standard templates (`vehicleTemplates.ts`); creates initial appliances based on selected vehicles and marks onboarding complete; integrated into signup flow via conditional render in `SignupPage.tsx` (shown only if onboarding not yet completed); completion tracked in localStorage via `onboardingUtils.ts` functions; responsive mobile-first design with RFS brand styling, light/dark mode support, progress bar, and back-navigation. *11 tests; frontend lint/typecheck/test suites green.*

- 2026-07-12: **Fire Break Calculator joins the subscription (#638, suite).** The `fireBreakEnabled` suite entitlement was reserved with no plan granting it, so the Fire Break Calculator could not be sold as part of the Station Manager subscription. **What shipped:** `fireBreakEnabled: true` on the Basic and AI Pro plans (`constants/plans.ts`, descriptions updated); a `grant:firebreak` ops script (`scripts/grantFireBreakEntitlement.ts`, dry-run by default) backfilling existing orgs whose stored entitlement snapshot predates the grant; entitlement-probe test coverage for the new grants. The sibling repo (`fireBreakCalculator`) gained SM sign-in and cloud saved plans validated against `GET /api/auth/me` — its origin must be present in `FRONTEND_URLS`. *2 new backend tests; suites green.*

- 2026-07-11: **Vehicle type templates + seeder test fix (#620).** Standard vehicle-type templates enable rapid multi-agency onboarding with 20 pre-provisioned checklists for NSW RFS, Fire & Rescue NSW, NSW SES, Marine Rescue NSW, and generic support vehicles; all seeded idempotently at startup and protected from org edits (built-in standards return 403 on PUT/DELETE). Fixed module-level seeder state preventing fresh database instances from seeding in tests by exporting a `resetForTesting()` function and calling it in test setup. **What shipped:** backend seeder service, 20 templates with stable item codes, `agency` + `seedVersion` fields on `VehicleType`, upsert methods on both DB twins, 403 protection for built-in standards, frontend agency badges and read-only built-in state. *Backend 914 tests green; frontend lint/typecheck clean; seeder tests: 8/8 pass.*
- 2026-07-10: **Roll out Bushie Tools design system v2.0 (#618).** New brand direction signed off from `handoff/Bushie Tools — Implementation Handoff.md`: Signal Red (`#D8232A`) replaces the old brand red, Hi-Vis Amber (`#F6A609`) replaces neon lime, a cool-slate neutral ramp, Archivo headings + JetBrains Mono for codes/slugs, and a new Signal Flame brand mark replacing the literal fire-station-building icon. **What shipped:** design tokens updated in `frontend/src/index.css` and mirrored in `aar-studio/css/rfs-tokens.css`; `.btn-primary/success/accent/danger` gradients flattened to solid fills; `lucide-react` added and emoji swapped for Lucide icons across the landing page, sign-in header, vehicle-check flow, auth/marketing/reports pages, and the station/crew-access admin consoles (some lower-traffic spots — achievement badges, activity tags, a few admin modals — still use emoji, queued below); terminology find-and-replace ("Vehicle Check", "Crew Access") applied to UI strings/nav labels; favicon/apple-touch/android-chrome/mstile raster set regenerated from the new `icon.svg` via headless Chromium, plus a proper multi-size `favicon.ico`; landing header rebuilt as a solid ink panel with an amber eyebrow (dropping the red→dark gradient + decorative circles); feature-card hover accent bar is now a flat per-module colour instead of a red→lime gradient; vehicle-check status pills use the `surface-*`/`text-*-strong` token pairs. *Backend: 905 tests green. Frontend: lint clean, typecheck clean, 554 tests green (2 fixed for the new icon/label). AAR Studio: 116/116 green. Full build passes. Verified in Chromium at iPad portrait/landscape, light + dark.*
- 2026-07-10: **Truck check — fix result-save 400, add a vehicle roster with cancel/finalise (#617).** Saving a check item 400'd (`POST /api/truck-checks/results` "Failed to create check result") whenever the item had a blank description, and once a check was left open from a previous session there was no way to cancel or finalise it from the landing screen — the vehicle stayed stuck showing "Join Check". **What shipped:** (1) `itemDescription` is now optional on the create-result path (validator + route), since checklist items legitimately carry no description and the workflow passes it straight through — the exact cause of the 400. (2) New `DELETE /api/truck-checks/runs/:id` cancels/abandons a run and cascade-deletes its results (both DB twins + `ITruckChecksDatabase.deleteCheckRun`; emits a `check-cancelled` socket event), exposed as `api.cancelCheckRun`. (3) The truck-check landing page is rebuilt as a **vehicle roster**: each card shows identity (make/model/year · rego), the last check result (e.g. `10/12 OK · 1 issue`), when and by whom, plus outstanding-issue count; in-progress vehicles offer **Resume / Finalise / Cancel**, and the board stays live via the `truck-check-update` socket event. *2 new backend tests (empty/omitted description → 201) + 2 (cancel run 204 + re-checkable, 404); 74 truck-check backend tests green; frontend truckcheck suites 76 green; both typechecks + lint clean; roster verified in Chromium at iPad portrait/landscape and the cancel flow driven end-to-end (DELETE 204 → vehicle returns to "Start Check").*
- 2026-07-06: **AAR Studio — colloquial sizes convert to metric (#615).** A test debrief describing a "tennis court"-sized fire flowed through to the report's key-stat size box as "1 Tennis Court", and findings kept colloquial/imperial measures ("3 miles down the road", "three footy fields") with no metric equivalent. **What shipped:** a new pure `js/lib/sizing.js` — a colloquial/imperial → metric table (tennis/basketball/netball courts, footy fields, ovals, olympic pools, acres, square miles/yards/feet; miles/yards/feet distances) with 2-significant-figure m²/ha/km² and m/km formatting. `annotateColloquialSizes()` runs as a deterministic secondary pass in `toFinding()` (extraction.js), appending "(≈ 260 m²)"-style equivalents after colloquial size phrases — context-gated so "staged at the tennis court" (a place, not a size) is left alone, and idempotent so a model-converted phrase isn't double-annotated. `normaliseSizeValue()` runs in `normaliseReport()` (reportGen.js) so a stat value that *is* a colloquial size becomes the metric figure ("1 Tennis Court" → "≈ 260 m²"). Both AI prompts (extraction + report) now instruct metric conversion inline (comparison kept, metric in parentheses; stats metric-only; quotes stay verbatim). *10 new tests (test/sizing.test.js); full AAR Studio suite 116 pass.*
- 2026-07-06: **AAR Studio — Present-mode live status + density-scaled cards + rebalanced consolidation (#615).** A facilitator recording a debrief and switching to the findings board's projector "Present" view had no way to tell recording/transcription or AI findings processing were still alive once the toolbar (and its "Analysing…" status text) disappeared behind the fullscreen CSS. Separately, the previous session's consolidation prompt over-corrected: distinct topics raised in one exchange (comms, size-up, staging location) were getting merged into a single finding instead of staying separate. **What shipped:** `js/views/board.js` renders a present-mode-only status strip — a recording dot + state text, a scrolling transcript tail (final segments plus live interim words), and a findings-processing spinner — wired directly to the `js/audio/live.js` and `js/analyse.js` singletons (a new `setAnalysisListener`/emit in `analyse.js`) so it updates without waiting for a store re-render, since interim words and analysis start/stop don't otherwise trigger one. Present-mode cards now always collapse the verbatim-quote explainer to reclaim vertical room, and font-size/padding step down through four density tiers (`present-density--roomy/cozy/tight/dense`) keyed to the busiest category column's count, with a flex-based one-screen layout (`css/app.css`) so a growing board keeps fitting the projector instead of only wrapping via grid auto-fit. The extraction prompt (`js/lib/extraction.js`) now instructs "consolidate by topic, not by segment": same-topic restatements across speakers still merge into one finding, but distinct topics discussed in the same breath stay as separate findings — aiming for ~3–8 findings per short segment rather than the prior 1–4 ceiling that pushed unrelated topics together. Full AAR Studio suite green (106 pass) — no browser available in this environment to verify the Present-mode UI visually.
- 2026-07-06: **AC-2 — ephemeral visitor sign-ins.** Continues the access & membership rework. Until now a walk-up guest with no `Member` record couldn't be logged against an event at all — the only sign-in path was tapping an existing member tile. **What shipped:** a new `POST /api/events/:eventId/visitors` (backend `events.ts`, validated by `validateAddVisitor`) adds an `EventParticipant` with a typed name, a synthetic `visitor-<uuid>` `memberId`, `memberRank: 'Visitor'`, and a new `isVisitor: true` flag (added to the shared `EventParticipant` type in `shared/domain-types.d.ts`) — deliberately **not** written to the members store, so it never counts toward `maxMembers`, gets no profile/history, and can't tap-to-repeat (each visit retypes the name). Unlike member check-in there's no toggle: every call inserts a fresh row, since two visitors may share a name. Implemented in both DB twins (`database.ts`'s `addEventVisitor` and its Table Storage mirror in `tableStorageDatabase.ts`). Frontend: `MemberNameGrid` gets a "+ Visitor" button beside the search bar that reveals a name input (Enter to submit, Escape to cancel); `SignInPage`'s new `handleAddVisitor` calls the API and refreshes the event; the roster (`CurrentEventParticipants`) shows a "Visitor" badge next to visitor names and removal works exactly like any other participant (the synthetic id is already unique). The member grid still only ever renders real `Member`s as tappable tiles — visitors were never added to that list, so no extra guard was needed. 6 new backend tests (`events.test.ts`: add, no-toggle-on-repeat-name, not-persisted-as-Member, missing-name 400, ended-event 400, roster visibility) and 13 new frontend tests (`MemberNameGrid.test.tsx`, `CurrentEventParticipants.test.tsx`); backend 38/38 event tests green, frontend 38/38 green on the two touched suites, both typechecks + lint clean.
- 2026-07-06: **Sign-in book responsive audit — two phone-layout bugs fixed.** A data-driven pass over the sign-in book (real seeded members + an active event, so the grid and event tabs actually rendered), scanning `/signin`, `/profile/:memberId`, and the personal-link `/sign-in` at 390/768/1024/1440px (plus 360/414/600 for the header) for horizontal overflow and 44px touch targets in Chromium. **Result: zero overflow anywhere; two real layout bugs found and fixed.** **(1)** The member profile hero never stacked on phones — `.profile-hero` is a CSS grid, but the ≤640px override tried to stack it with `flex-direction: column`, which is inert on a grid, so the avatar and body stayed cramped side-by-side; fixed with `grid-template-columns: 1fr` + centring, and capped the full-UUID `🆔` chip so it can't overflow (`UserProfilePage.css`). **(2)** On phones the event tab and "+ New Event" button ran into each other (owner-reported) — the collapse button + tab + full-size lime button fought for one row, starving the `flex:1; overflow-x:auto` tab below its own width so it clipped its end `✕` and butted against New Event; the header now wraps with an 8px gap and at ≤480px "+ New Event" drops to its own full-width row so the tab renders in full (`MemberNameGrid.css`). Both verified in Chromium across the width range; MemberNameGrid + profile component tests green, lint clean. Details in [RESPONSIVE_UI_REVIEW_20260704 §Addendum 2](history/reviews/RESPONSIVE_UI_REVIEW_20260704.md). Three secondary sub-44px header icons (⚙️/🌙 ~32px, event ✕ 28px) documented as known/deferred (tight tab strip; not a safe blind bump).
- 2026-07-04: **AC-1 — the personal member link becomes a real credential.** Completes the access & membership rework's core gap: checking in via a personal link (`/sign-in?user=<memberId>&station=<stationId>`) used to dead-end — the visitor's browser had no JWT, brigade token, or Device token, so `flexibleAuth`/`requireSession` 401'd every follow-up read and the link's "Done" button routed home rather than into the (now-gated) `/signin`. **What shipped:** `POST /api/checkins/url-checkin` now mints a short-lived (8h), station-scoped **member-session** JWT (`{ memberId, brigadeId, stationId, credentialType: 'member-session' }`, resolved via a `getStationById` lookup for the brigadeId) on every successful or already-checked-in response. A new `validateMemberSession()` in `middleware/flexibleAuth.ts` recognises it via the `X-Member-Session` header, granting the same **station-wide read** scope as a brigade/kiosk token (never more) — wired into both `flexibleAuth()` and `requireSession()`, so the member can now load the live sign-in book (grid, active event) exactly like a kiosk device would. Frontend: `utils/memberSession.ts` stores the token (session-scoped), `api.ts`'s `getHeaders()` forwards it, `SignInLinkPage.tsx` stores it on success and its button now says "Go to Sign-In Book" → `/signin` (was "Done" → `/`), and `AccessRoute` gains `hasMemberSession()` as a fourth allowed path alongside signed-in/kiosk/demo. The credential is deliberately narrow: read-only, station-scoped, expires on its own — never a path to admin actions. 5 new backend tests (`checkins.test.ts`'s sessionToken assertions + `dataAccessProtection.test.ts`'s member-session gate coverage) and 4 new frontend tests (`memberSession.test.ts`, extended `AccessRoute.test.tsx`); backend 894 pass/1 skip, frontend 544 pass/16 skip, both typechecks + lint clean.
- 2026-07-04: **AC-5 — first-class Device accounts, replacing the anonymous kiosk token (owner direction, brought forward from the queue).** Continues the access & membership rework. The brigade device code `AccessRoute` checks (kiosk `?brigade=<token>`) was an anonymous `BrigadeAccessToken` UUID with no name, type, or audit trail — and its store was in-memory only, so tokens never survived a Table Storage-backed prod restart. **What shipped:** a first-class `Device` model (`backend/src/types/index.ts`) — `id`, `organizationId`, `stationId`, `type: 'kiosk'|'tablet'|'phone'|'wearable'`, `name`, `token`, `status: 'active'|'revoked'`, `lastSeenAt`, `expiresAt` — with in-memory (`deviceDatabase.ts`) and Azure Table Storage (`tableStorageDeviceDatabase.ts`) twins selected by `deviceDbFactory.ts` (mirrors `vehicleTypeDbFactory.ts`'s pattern), so devices now persist in production. New `GET/POST /api/devices`, `PATCH /api/devices/:id` (rename/revoke/reactivate), `DELETE /api/devices/:id` (owner/admin, org-scoped), and a public `POST /api/devices/validate`. **The integration that makes this real, not just a parallel unused API:** a new `services/kioskAccessResolver.ts` tries the `Device` store first and falls back to the legacy `BrigadeAccessToken` store — wired into `middleware/kioskModeMiddleware.ts` (URL `?brigade=` gate), `middleware/flexibleAuth.ts` (the `X-Brigade-Token` header path that actually gates `/api/members`, `/api/events`, `/api/activities`, `/api/checkins`), and `POST /api/brigade-access/validate` (what the frontend's `validateKioskToken()` calls). So a kiosk enrolled via the new system can sign in, check in members, and run activities exactly like a legacy token — nothing about the kiosk URL shape changed. Frontend: `admin/brigade-access` now has a "Devices" section per station (enroll with a name + type, rename inline, revoke/reactivate, remove, copy/QR the kiosk URL) alongside the existing legacy token UI, which is untouched. New `Device` type + `api.ts` methods (`getDevices`/`createDevice`/`updateDevice`/`deleteDevice`). 29 new backend tests (device CRUD, ownership scoping, expiry/revocation, and the resolver's Device→legacy fallback + station→brigadeId lookup) and 7 new frontend tests; backend 889 pass/1 skip, frontend typecheck+lint clean. `maxDevices` enforcement remains intentionally off per #574 (devices dropped from pricing) — this ships the entity/CRUD/audit-trail value without changing billing.
- 2026-07-04: **Access model rework — gate the sign-in book behind a real credential (owner direction).** First increment of a broader rethink of how anonymous access, membership, and subscriptions fit together (see MASTER_PLAN "Access & membership rework"). Until now `/signin` rendered for *anyone* — with no brigade code it silently fell back to the demo/default station and showed the book, so the app opened by typing the bare URL. **What shipped:** a new `AccessRoute` gate (`frontend/src/components/AccessRoute.tsx`) wraps `/signin` and only lets a visitor in when they (a) are signed in, (b) arrived on a brigade device via its unique code (kiosk `?brigade=<token>`), or (c) are in the public demo. Anyone else is redirected to the front door (`/`), which shows the marketing page when logged out. The public demo is now explicit: a prominent "Try the demo" button on the marketing hero, and a small `demoMode` util (`frontend/src/utils/demoMode.ts`) that makes `?demo=true` sticky for the browser session so the gate keeps letting the demo through as it navigates between modules (the query param drops on navigation). The personal member-link success screen (`SignInLinkPage`) now ends with "Done" → home instead of pushing into `/signin` (a one-member check-in link isn't a pass into the full book — that's follow-on work). `AccessRoute` is deliberately stricter than `FeatureRoute`: FeatureRoute asks "does this brigade's plan include the module?", AccessRoute asks "is this visitor allowed in at all?" — compose AccessRoute (outer) → FeatureRoute (inner). Real production kiosks already use the `/signin?brigade=<token>` URL, so they're unaffected. 9 new frontend tests (`AccessRoute.test.tsx`, `demoMode.test.ts`); full frontend suite green (533 pass). Verified end-to-end in Chromium: bare `/signin` → marketing page; `?demo=true` → sticky → book renders; `?brigade=` → passes the gate. **Queued follow-ons** (MASTER_PLAN AC-1…AC-4): personal member link as a real credential (link carries brigadeId+memberId, establishes a read session), ephemeral visitor sign-ins, and a start-session-then-share-a-join-link model for truck checks before gating `/truckcheck` the same way.
- 2026-07-04: **Truck check: cross-brigade comparative reporting (Q5).** The point of locked standard checklist items with stable `itemCode`s (TC-D1) is comparability — until now nothing actually compared across stations. **What shipped:** `GET /api/reports/truckcheck-comparative` (`backend/src/routes/reports.ts`) aggregates `CheckResult`s by `VehicleType.code` (falling back to the legacy free-form `Appliance.vehicleType` slug) then by `itemCode`, returning a per-station breakdown (`totalChecks`/`doneCount`/`issueCount`/`skippedCount`/`passRate`) for each; results with no `itemCode` (pre-TC-D1 legacy data) are skipped since they aren't comparable, and a vehicle type left with no comparable items is dropped from the response. Mirrors the existing `cross-station/*` endpoints' `stationIds` query param, but goes further: when an authenticated org context is present, requested `stationIds` are filtered down to stations actually owned by the caller's organization first, rather than trusting the client-supplied list outright (the existing cross-station endpoints don't do this — flagged, not fixed, as out of scope here). Frontend: `TruckCheckComparativePage.tsx` (new, at `/truckcheck/comparative`, linked from the Admin Dashboard's tab strip) reuses `MultiStationSelector`, fetches the last 90 days, and renders one table per checklist item with pass-rate highlighting. 3 new backend tests (aggregation + org-scoping + legacy-data skip) and 4 new frontend tests; both suites green, both typechecks + lint clean.
- 2026-07-04: **Sign-in book responsive check (addendum to the cross-app UI review).** Same automated scan run against `/signin` with the dev backend serving real station/event data. **Result: clean** — `SignInPage.css`'s four tuned breakpoints (1400/1200/1024/768px) hold up with zero horizontal overflow at phone/tablet/desktop widths. Three secondary icon buttons (`theme-toggle-btn`, `admin-menu-btn`, `event-end-btn`) measure under the 44px touch-target minimum but are low-frequency actions, not the primary sign-in flow; noted in the review doc, not fixed (`event-end-btn` in particular is sized for a tight tab strip and a blind bump risks the layout). No code changes.
- 2026-07-04: **Cross-app responsive UI review — phone-width overflow + touch-target fix.** Per owner direction to factor desktop/tablet/mobile UI layers into the next queue items: an automated Playwright audit (390/768/1024/1440px viewports, every route in both apps, scripted overflow + 44px-touch-target checks) found aar-studio's stylesheet has only 3 `@media` rules total (vs. `frontend/`'s 65 responsive CSS files) and turned up two real bugs traced to this session's earlier session-clarity work. **What shipped:** [`RESPONSIVE_UI_REVIEW_20260704.md`](history/reviews/RESPONSIVE_UI_REVIEW_20260704.md) documents the audit; **UI-1** — `.topbar__brand` laid the "AAR Studio" wordmark and the active-review chip out as a non-wrapping flex row, overflowing the viewport by 84px on every aar-studio route at phone width whenever a review was open; now `flex-wrap: wrap`. **UI-2** — the chip's ✕ close button was an 18×19px touch target; now padded to the shared 44px `--tap-min-sm` hit area without inflating the glyph. `frontend/` came back clean at every route/viewport tested. Verified via the automated scan (zero overflow/touch-target violations post-fix) plus a Chromium screenshot; full AAR Studio suite green (106 pass, no test changes needed — CSS only). **Queued (Q19, not urgent):** aar-studio has no shared responsive-breakpoint convention to lean on as it grows — today's clean scan owes more to auto-fit grids degrading gracefully than to deliberate design.
- 2026-07-04: **AAR Studio — Q3 remaining batch (collab promise + lows).** Worked down the [AAR review](history/reviews/AAR_STUDIO_REVIEW_20260703.md)'s remaining queue. **What shipped:** **AAR-21** — room notes fed the AI but appeared in no export; the combined HTML report and Markdown now carry a "Room notes" appendix (label + time + text), so contributors see their words in the record. **AAR-20** — "Print / PDF" printed the preview iframe (unreliable on iOS Safari); it now opens the snapshot as a standalone document in its own window with `@page` margins (*physical-iPad verification still pending*). **AAR-22** — the snapshot preview rebuilt on every keystroke; per-keystroke edits now debounce ~250 ms. **AAR-24** — untitled exports collided as `aar-snapshot.html`; `sessionFilename` now falls back to the incident/created date. **AAR-18** — finding deletion was instant/unrecoverable while review deletion asks to confirm (inverted weight); `toast()` gained an optional action button and deletes now offer "Undo" (restores at the original position), and the inline editor rejects blank saves. **AAR-17** — `mergeFindings` dropped a unit attribution the kept finding lacked (now `keep.unit || drop.unit`). **AAR-16** — dropped the always-undefined `stationId` from the sync body. **AAR-13** — the participant join page's status was write-once ("Connected" even after the phone slept); it now reflects live socket connect/disconnect via a new `collab.onConnectionChange`. **AAR-14** — the "share tab / system audio" capture option is hidden where `getDisplayMedia` is unavailable (iPad Safari). **AAR-12** accepted-and-documented (findings from notes carry note ids in `segmentIds`, but no consumer resolves ids against `segments`). **AAR-4** — "See an example" was only a footer link; the zero-reviews empty state now leads with a prominent "See what a finished review looks like" CTA. **AAR-5** — a quiet, dismissible signed-out hint ("Sign in to back up & share…", with Sign in / Not now) since cloud backup is invisible when signed out. Also fixed a latent `parent.append(null)` → literal "null" render bug surfaced during browser verification (new `mount()` helper in `ui.js`). 5 new tests (room-notes export, date-fallback filename, merge unit, + the earlier trigger test); AAR Studio suite 106 pass; delete→Undo, signed-out home, and the reworked board/edit/home all verified in Chromium at iPad-portrait width. That clears the AAR review's entire remaining queue; only physical-iPad verification of the AAR-20 print path is left (batched into the Q3 voice-agent on-device session). The MASTER_PLAN queue was renumbered after removing the now-complete AAR remaining-batch item.
- 2026-07-04: **AAR Studio — insight-quality + session-clarity rework (owner direction).** Owner's next hero pass: make AAR Studio surface *consolidated wisdom* like a facilitator, not rephrase every sentence, and stop confusing people about which review they're in. **What shipped:** (a) **Insight consolidation** — extraction no longer fires on a fixed 45 s timer over narrow 450-word chunks with a "one discrete point" prompt (which produced 30+ per-utterance findings for a short debrief). It now waits for the discussion to *settle* (a 15 s quiet gap after the last words, backlog banked; a 600-word ceiling still processes an unbroken monologue), reads a whole exchange (chunk range 450 → 1400 words), and runs a rewritten "expert facilitator" prompt that CONSOLIDATES a back-and-forth into ~1–4 substantial findings rather than many thin restatements (`js/lib/extraction.js`, `js/analyse.js`). (b) **Edit insights, not transcript** — board finding cards are now tap-to-edit (keyboard-accessible), and the Review/Edit page leads with "Edit findings", tucking raw-transcript + speaker correction into a collapsed "Advanced" section (`js/views/board.js`, `js/views/review.js`). (c) **Session clarity** — an active-review chip in the topbar ("📋 <title> ✕", tap to open / ✕ to close) and a "● Currently open" flag + explicit "Open review →" affordance on Home cards (`js/main.js`, `js/views/home.js`). Also fixed a latent bug the browser verification exposed: several `parent.append(null)` sites (empty finding quote, hidden merge panel, the AAR-10 banner) rendered the literal string "null" on the board — added a null-safe `mount()` helper (`js/ui.js`) and routed the affected renders through it. Verified in Chromium at iPad-portrait width (screenshots: active chip, active-card flag, findings-led Edit page, no stray "null"). 1 extraction test updated; full AAR Studio suite green (103 pass).
- 2026-07-03: **AAR Studio hero fix — meter speech tokens by meeting, not by vend (AAR-11).** Finishes the [AAR Studio hero review](history/reviews/AAR_STUDIO_REVIEW_20260703.md)'s Q1 batch. Every `/api/ai/speech/token` vend counted as one metered AI session, but the AAR-6 proactive token refresh (every 8 min) and the AAR-8 auto-reconnects re-vend within a single meeting — so a normal 30–90 min AAR could burn several sessions' allowance. **What shipped:** the client now sends the review's session id with each vend (`js/audio/live.js`'s `fetchSpeechToken`); backend `countUsage` dedupes usage rows sharing a `sessionId`, and the speech-token allowance gate uses a new `hasRecordedSession` to let a re-vend for an already-counted review pass without consuming (or re-charging) a session — both the in-memory and Table Storage `IUsageDatabase` twins updated in lockstep. `docs/registers/api_register.json` documents the `sessionId` request field. 2 new backend tests (usage dedupe + repeated-vend route behaviour); backend suite green. **This closes the entire Q1 "AAR hero polish" batch** (AAR-1/2/3/6/7/8/9/10/11/15/19/23).
- 2026-07-03: **AAR Studio hero fix — surface cloud-sync conflicts while editing (AAR-15).** Closes the last "confident live demo" concern from the [AAR Studio hero review](history/reviews/AAR_STUDIO_REVIEW_20260703.md)'s Q1 queue. `pushSession` already reported `{ conflict: true }` on a 409 (the team's cloud copy was edited more recently elsewhere), but the only live caller — the debounced backup push — fired and forgot it; a losing facilitator's edits stayed local-only for the whole session with zero indication, only surfacing later as a "☁ Newer version" flag on the Home screen if they happened to look. **What shipped:** `js/store.js`'s `scheduleCloudBackup` now latches a conflict into a session-scoped `syncConflict`, with `loadLatestFromTeam()` (pulls the team's copy over local edits) and `keepMineAsCopy()` (forks the session to a new id, dropping the stale local entry under the old one) to resolve it; `js/main.js` renders a persistent banner ("A newer team copy exists — your edits are saving to this device only · Load latest / Keep mine as a copy") on every view, not just Home. Full AAR Studio suite green (103 pass) — `store.js`/`main.js` are browser-only (localStorage/DOM) and untested under `node --test`, consistent with the project's existing coverage boundary.
- 2026-07-03: **AAR Studio hero fix — replace native prompt/confirm with in-app dialogs (AAR-2).** Continues the [AAR Studio hero review](history/reviews/AAR_STUDIO_REVIEW_20260703.md)'s Q1 queue. Native `window.prompt`/`window.confirm` are unstylable, break the brand, and are suppressed in some kiosk/webview contexts — the main SPA had already purged them. **What shipped:** `js/ui.js` gains `confirmDanger()` and `promptDialog()`, both Promise-returning and built on a new `#confirm-dialog` `<dialog>` element (`index.html`) alongside the existing `#settings-dialog` pattern; every call site now awaits them — `views/home.js` (rename, remove-from-device, delete-for-everyone), `views/capture.js` (clear transcript), `views/report.js` (regenerate, discard report). No native dialogs remain in `aar-studio/js`. Full AAR Studio suite green (103 pass) — `ui.js` is DOM-only and untested under `node --test`, consistent with the project's existing coverage boundary for browser-only files.
- 2026-07-03: **AAR Studio hero fix — latch persistent AI failures (AAR-10).** Continues the [AAR Studio hero review](history/reviews/AAR_STUDIO_REVIEW_20260703.md)'s Q1 queue. A 401/402/403 (sign-in required, allowance exhausted, plan doesn't include AI) during live auto-extraction used to raise an identical 8-second error toast on every 45-second retry for the rest of the meeting. **What shipped:** a new `isPersistentAiError()` (`js/lib/llm.js`) classifies 401/402/403 as persistent (429/503/network errors stay transient, since those can clear up on their own); `analyseNow` (`js/analyse.js`) latches a persistent failure into a module-level `persistentError` instead of re-toasting, and clears it automatically on the next successful pass; `maybeAutoExtract` pauses entirely while one is latched; Capture and Board views render a dismissible `.ai-error-banner` with the message/hint, and dismissing lets auto-extraction try again. 1 new test (`llmGateway.test.js`); full AAR Studio suite green (103 pass).
- 2026-07-03: **AAR Studio hero fixes — gateway-appropriate copy (AAR-9/23) + GPS-fallback location (AAR-3).** Continues working down the [AAR Studio hero review](history/reviews/AAR_STUDIO_REVIEW_20260703.md)'s Q1 queue. **What shipped:** (1) **AAR-9** `js/audio/speech.js` — the speech `canceled` handler's hint ("Check the Speech key/region in Settings") only makes sense for a BYO-Azure developer; `createTranscriber` now branches on a local `gatewayMode` check (`Boolean(settings.authToken)`) so a bushie on the built-in AI gets "Try again in a moment — if it keeps happening, tell your administrator" instead; (2) **AAR-23** `js/views/report.js` — same audience mismatch in the report empty-state ("Generation uses the report deployment from ⚙ Settings"), now branches on `usesGateway(getSettings())`; also fixed a stale header comment claiming AI report generation hadn't shipped yet; (3) **AAR-3** `js/lib/model.js`/`js/store.js`/`js/lib/extraction.js`/`js/analyse.js`/`js/views/setup.js` — quick-start's device-captured GPS coordinates (`"-35.1234, 149.5678"`) used to permanently occupy `incident.location`, since `mergeMetadata` only fills blank fields and the AI's real place name could never land. A new `incident.locationIsAuto` flag marks a GPS-fallback value; `setIncidentLocation` sets it, `mergeMetadata` now treats a still-auto location as fillable (clearing the flag once a real name lands), `fillMetadataFromDiscussion`'s blank-check and `setup.js`'s manual location field both clear/respect the flag correctly. 1 new test (`friendlyFlow.test.js`); full AAR Studio suite green (102 pass).
- 2026-07-03: **AAR Studio hero fixes — reliability (AAR-6/8/1/7) + report attribution (AAR-19).** Implements the top-ranked findings from the [AAR Studio hero review](history/reviews/AAR_STUDIO_REVIEW_20260703.md) — the ones between AAR Studio and a confident live demo. **What shipped:** (1) **AAR-6** `js/audio/speech.js`/`js/audio/live.js` — the gateway Azure Speech token (valid ~10 min) now refreshes proactively every 8 minutes via a new `updateAuthToken()` on the transcriber, so a real 30–90 min AAR no longer dies mid-meeting; (2) **AAR-8** same files — a transcriber failure no longer tears down the mic/worklet/backup recording on first error: a new `reconnecting` state attempts up to 4 backed-off automatic reconnects (audio graph and backup recording keep running throughout), only giving up — with a clear message — after exhausting retries; capture.js shows an amber "Connection interrupted — reconnecting…" status instead of silently falling back to idle; (3) **AAR-1** `js/store.js`/`js/views/home.js` — review deletion split into `deleteSessionLocal()` ("Remove from this device") vs `deleteSessionEverywhere()` (signed-in-only "Delete for everyone" with an explicit team-wide warning) — previously the only delete button silently wiped the brigade's shared cloud copy too; (4) **AAR-7** `js/lib/collab.js` — the facilitator/participant now re-announce themselves to the Socket.io room on every reconnect (room membership doesn't survive a transport reconnect), so room notes keep flowing after a network blip instead of vanishing while the sender still sees "sent"; (5) **AAR-19** `js/lib/exports.js` — every export (snapshot HTML, combined report, Markdown summary) now carries a "Produced with AAR Studio · Bushie Tools" attribution line; investigation found the report's colour palette already matches the current brand tokens exactly, so the finding (and this fix) narrowed to attribution only. 9 new tests (`speechAuth.test.js`, `collabReconnect.test.js`, extended `exports.test.js`); full AAR Studio suite green (101 pass). Flagged for a follow-up: AAR-11 (the token refresh/reconnects now call `/api/ai/speech/token` more often, and the backend currently meters every vend as a separate session) is deliberately left open — billing semantics, not a reliability bug.
- 2026-07-03: **Fix LL-1…LL-4 — post-login lands on the app picker, not an admin console.** Implements the [sign-in/landing review](history/reviews/LOGIN_LANDING_REVIEW_20260703.md). **What shipped:** (1) `LoginPage.tsx` now defaults `from` to `/` instead of `/admin/stations` (deep links still honour `state.from` via `ProtectedRoute`); (2) an already-authenticated visitor to `/login` is redirected immediately instead of seeing the form; (3) `SignupPage.tsx` sends a plain (no `?plan=`) signup to `/` with a welcome toast, while billing-intent signups still land on `/admin/organization` or Stripe Checkout unchanged; (4) new shared `components/AdminNav.tsx` (+ CSS) — a lightweight top bar with a Bushie Tools home link, Stations/Brigade Access/Organization nav (active-state highlighted), and the signed-in username + sign-out — replaces the three admin pages' ad-hoc `← Home` text links and dead-end feel. 15 new tests (`LoginPage.test.tsx`, `AdminNav.test.tsx`, updated `SignupPage.test.tsx`); full frontend suite green (520 pass/16 skip), typecheck + lint clean. *Screenshot capture (iPad portrait/landscape, PR convention) deferred — no browser in this environment; flagged for a follow-up on-device pass.*
- 2026-07-03: **AAR Studio hero review + sign-in/landing review (findings only, no code).** Owner directive: AAR Studio is the headline feature — reviewed its UX and function to that bar, plus the sign-in/landing sequences. **What shipped:** [`AAR_STUDIO_REVIEW_20260703.md`](history/reviews/AAR_STUDIO_REVIEW_20260703.md) — full read of all ~4,300 lines of `aar-studio/` source + backend seams, 24 findings (AAR-1…24) with severity, evidence and direction, topped by AAR-6 (Critical: gateway speech token never refreshed — Azure tokens last ~10 min, so live transcription dies mid-debrief), AAR-8 (any speech error tears down capture including the local backup recording), AAR-1 (local review delete silently deletes the team's cloud copy), AAR-7 (Socket.io room membership not re-established on reconnect — room notes silently lost), AAR-19 (exported report still carries the pre-rebrand palette and no product attribution); plus a "what is already hero-grade" section and a full implementation ranking. [`LOGIN_LANDING_REVIEW_20260703.md`](history/reviews/LOGIN_LANDING_REVIEW_20260703.md) — LL-1…4: post-login default is `/admin/stations` instead of the app picker (`LoginPage.tsx:32`), admin pages lack shared navigation/logout, plain signups land on the Organization console, `/login` renders for already-authed users. **Master plan updated:** AAR elevated to "THE HERO" on the status board; queue re-ranked — Q1/Q2 (AAR hardening + polish), Q3 (sign-in/landing), Q5 (AAR remaining batch) now lead; voice-agent and other tracks shifted down accordingly. *Review docs + plan edits only; implementation is queued for follow-up agents.*
- 2026-07-03: **Docs restructure — plan + registers + wiki.** The `docs/` folder had grown ~35 loose top-level files plus three ad-hoc subfolders, and the master plan mixed a live roadmap with years of completed issue write-ups. **What shipped:** `docs/` now holds exactly three things — `MASTER_PLAN.md` (rewritten as forward-intent only: product snapshot, feature status board, one prioritised queue Q1–Q18, open decisions D1–D5, risk register), `registers/` (`api_register.json`, `function_register.json`, `openapi.yaml`), and `wiki/` (`developer/` with 20 migrated guide/reference pages + this changelog + `history/` for the old archive/current_state/implementation-notes trees and the preserved v4 plan; `user-guide/` with a new 14-page end-user guide covering sign-in, profiles/achievements, truck checks, voice check, reports, AAR Studio, admin, kiosk/PWA, troubleshooting). `CLAUDE.md` rewritten as enduring standards only (docs-structure rule, progress-tracking rule pointing dated entries here); `.github/copilot-instructions.md` deleted (CLAUDE.md is the single agent guide); all repo references (README, PR template, workflows, scripts, code comments) updated to the new paths; `scripts/validate-registries.sh` retargeted. *Registry validation green; both typechecks clean; all relative links in live docs verified.*
- 2026-07-03: **A3 code-review hardening — all 16 findings fixed (F1–F16).** Closes out the review filed 2026-07-02 (`docs/current_state/CODE_REVIEW_A3_20260702.md`) before wider rollout of the voice agent. **Security/tenancy:** WS upgrade now resolves the appliance's owning station/org and 403s a caller from a different org (F1); `GET /api/agent-sessions/:id[/turns]` 404s instead of serving another org's transcript, via a new `organizationId` field on `AgentSession` set at creation (F2); WS upgrade now checks `aiEnabled` the same as REST (F3); per-user connection cap (3, 429), per-session turn cap (60) and text-length cap (2000 chars) (F4); `maxPayload` 256 KiB on the `ws` server (F5); Origin header check on the upgrade (F6). **Data integrity:** `tableStorageAgentSessionDatabase.updateSession` now does an ETag-conditional `Replace`, retrying up to 3 times on a 412 conflict instead of an unguarded read-modify-write — closes the race between `complete_run` and the WS close handler racing to write `AgentSession.status` (F10). **Stability/UX:** dev WS URL now targets the backend origin instead of Vite's (F7); mic capture resumes a suspended `AudioContext` and playback failures surface a status message instead of failing silently (F8); client reconnects with backoff (1s→8s, 5 attempts) and resumes the same session via `resumeSessionId`, with a server pong-timeout closing dead sockets (F9); `VoiceCheckPage` gained an `onBusy` handler so a `busy` frame shows a retry message instead of permanently disabling the UI (F11). **Correctness:** `AgentToolExecutor.ensureRun` now joins an already-active `CheckRun` on the appliance instead of always creating a new one, mirroring manual "Join Check" (F12); silence/no-match replies are now persisted to the transcript (F13); a repeated `audio-start` no longer drops buffered audio (F14); the WS message handler no longer lets a post-parse exception crash the connection silently (F15); the hold-to-talk button uses the Pointer Capture API so natural finger drift no longer truncates a recording (F16). **Conventions cleanup:** `docs/function_register.json` (→ v1.4.0) and `docs/api_register.json` (→ v1.13.0) now document the agent-session services/routes and the raw `/ws/agent-check` protocol (previously undocumented since inc 1); composer touch targets raised 48px→60px (kiosk rule); `agentSpeech.ts`'s `escapeXml` strips XML-illegal control chars before entity-escaping so a stray byte in an LLM reply can't make Azure reject the whole SSML request; `agentLoop.ts`'s completion check now parses the tool result's typed `completed` field instead of substring-matching the raw JSON text. Every fix shipped with new regression tests targeting the specific failure scenario. *Backend 855 pass/1 skip, frontend 513 pass/16 skip, AAR Studio 93 pass; both typechecks + lint clean; full build green.* **Remaining:** iPad on-device verification of the F8 audio fixes; optional continuous-listening (VAD) upgrade is unrelated follow-on work, not a hardening gap.
- 2026-07-02: **A3 inc 3 — voice layer: server-side Azure Speech + PWA push-to-talk UI.** The voice agent is now usable by voice end-to-end: hold-to-talk in the PWA → 16 kHz PCM over the WS → server-side Azure STT → tool loop → server-side Azure TTS reply played back. **Design call:** REST short-audio recognition with push-to-talk utterances (≤60 s) instead of the streaming Speech SDK — keeps the D3 backend-proxy decision, adds zero dependencies, is fully unit-testable, and press-and-hold suits a noisy engine bay; continuous/VAD streaming can replace `recognizeSpeech` behind the same WS frames later. **What shipped:** (1) `services/agentSpeech.ts` — `recognizeSpeech` (STT short-audio REST, en-AU, silence→empty-not-error), `synthesizeSpeech` (TTS REST, SSML-escaped, `AZURE_SPEECH_VOICE` default en-AU-NatashaNeural, MP3 out), `buildWavFile` (server wraps the raw PCM chunks in the RIFF header); reuses the existing `AZURE_SPEECH_KEY/REGION`. (2) WS protocol (`routes/agentCheck.ts`): `audio-start` → binary PCM chunks (2 MB cap = stuck-key guard) → `audio-end` → `transcript` frame → tool loop → `agent-text` + `agent-audio` header + one binary MP3; `user-text` gained a `speak` flag; canned prompts for silence/empty capture (no LLM call). (3) Frontend `features/truckcheck/voice/`: `audioEncoding.ts` (pure downsample-to-16k + Float32→PCM16, tested), `voiceAgentClient.ts` (WS wrapper, injectable socket, agent-audio↔binary pairing), `audioIO.ts` (getUserMedia/ScriptProcessor capture + playback shell; coverage-excluded like the other browser-only utils), `VoiceCheckPage.tsx` at `/truckcheck/voice/:applianceId` behind `FeatureRoute aiEnabled` (hold-to-talk ≥72 px, live transcript log, typed fallback, summary link on completion, mic-denied recovery); 🎙 Voice Check button on the truck-check hub cards (AI-plan orgs only). 35 new tests: 9 speech-adapter, 13 WS integration over a real socket (first tests of the WS endpoint — auth, text turns, audio turns, caps, abort-on-disconnect), 13 frontend (encoding/client/page). *Backend 817 + frontend 500 tests pass; all coverage gates green; both typechecks + lint clean.* **Remaining (A3 polish):** register the agent WS/REST surface in `api_register.json`; iPad screenshots for the PR; optional continuous-listening upgrade; A4 (offline + vision) unchanged.
- 2026-07-02: **A3 inc 2 — voice-agent tool loop (text mode).** The agent can now actually run a truck check end-to-end over the `/ws/agent-check` WebSocket, in text mode (STT/TTS streaming is inc 3; text stays as the permanent debug surface). **What shipped:** (1) `services/effectiveChecklist.ts` — `resolveEffectiveChecklist` extracted from the truck-checks route so the agent merges the checklist identically to the manual workflow; (2) `createCheckRun` gained a trailing optional `runDetails?: { source, agentSessionId }` (interface + **both** twins, persisted as Table columns) so agent-started runs carry provenance; (3) `services/aiGateway.ts` — `ChatMessage` type + `tools`/`toolChoice` passthrough on `chatCompletion` (existing callers unchanged); (4) `services/agentTools.ts` — the five design-contract tools (`get_appliance_context`, `record_result`, `flag_issue`, `next_unchecked_in_zone`, `complete_run`) as an `AgentToolExecutor` bound to one AgentSession, with lazy CheckRun creation (linked both ways), item resolution by id/itemCode/name, re-record-updates-not-duplicates, zone-scoped next-item, and run/session finalisation; (5) `services/agentLoop.ts` — `runAgentTurn` drives Azure OpenAI function calling (max 8 tool iterations/turn), persists every step as `AgentTurn`s (user/tool/agent/system), replays only *text* turns as history (live state re-grounds via tools, so reconnects never desync); (6) `routes/agentCheck.ts` — `user-text` → loop → `agent-text` frames, `end-session`, one-turn-at-a-time busy guard, and close now marks an un-finished session `aborted` (only `complete_run` yields `completed`). 21 new tests (13 tools + 8 loop with mocked provider); *795 backend tests pass, coverage gates green, typecheck clean.* **Remaining (inc 3):** Azure Speech STT/TTS streaming over the same WS; PWA voice UI; register the agent API surface in `api_register.json` once the WS contract is final.

### June 2026 Stabilization
- 2026-06-29: **A3 inc 1 — voice-agent backend plumbing.** D3 streaming-voice decisions resolved (backend proxy audio routing; Azure OpenAI function calling; same App Service with 30-s WS pings for 60-s idle timeout). **What shipped:** `AgentSession`/`AgentTurn` types in `backend/src/types/index.ts` (+ `CheckRun.source`/`CheckRun.agentSessionId` optional fields); in-memory twin `services/agentSessionDatabase.ts` + Table Storage twin `services/tableStorageAgentSessionDatabase.ts` (two-table: `AgentSessions` PK=applianceId RK=id, `AgentTurns` PK=sessionId RK=zero-padded-sequence+'-'+uuid); factory `services/agentSessionDbFactory.ts`; WS stub `routes/agentCheck.ts` — raw WS at `/ws/agent-check` (JWT auth, 30-s ping keepalive, session created on connect / completed on disconnect, binary audio ignored in stub); REST `GET /api/agent-sessions/:id` + `GET /api/agent-sessions/:id/turns` (behind `requireFeature('aiEnabled')`); WS upgrade handler attached to `httpServer` before Socket.io; factory init in `initializeDatabasesInBackground`. 20 new backend tests (DB unit + REST route). *Backend typecheck clean; 754 tests pass.* Inc 2 will wire Azure Speech STT + Azure OpenAI tool-loop.
- 2026-06-22: **Deploy perf — switch Azure deploy to run-from-package (~8 min → <1 min).** Diagnosed the "Deploy to Azure Web App" step taking 9m45s; the OneDeploy log showed 8m07s of pure server-side time with an empty `build_summary` (no Oryx build). Root cause: with `WEBSITE_RUN_FROM_PACKAGE` unset, OneDeploy **extracts `backend/node_modules` (tens of thousands of small files) onto the `/home` Azure Files share** on a B1 plan — the per-file write to a network mount is the whole cost. **What shipped:** (1) `infra/modules/appservice-linux.bicep` sets `WEBSITE_RUN_FROM_PACKAGE=1` so the zip is mounted read-only instead of extracted; (2) `backend/src/services/logger.ts` redirects winston's production File transports off the now-read-only wwwroot `logs/` path to writable/persisted `/home/LogFiles/app` (override via `LOG_DIR`), wrapped in try/catch so log setup can never crash startup; (3) `.github/workflows/ci-cd.yml` adds `WEBSITE_RUN_FROM_PACKAGE=1` to the existing post-deploy app-settings step so it takes effect *after* the logger fix is already deployed (avoids the read-only-fs/EROFS race on the first run). The `rfsFacilitiesParser` blob-CSV writer is *not* a blocker — the build always ships the seed CSV so `fs.existsSync` short-circuits before any write in prod (a side effect: the blob-override path is effectively dead in prod today — noted for later). Backend typecheck clean; 13 logger tests pass. Stale "Windows/IIS/iisnode + Oryx build" framing in `docs/AZURE_DEPLOYMENT_OPTIMIZATION.md` and `docs/ci_pipeline.md` is now inaccurate (the app is Linux `NODE|22-lts` running `bash startup.sh`) — flagged for a follow-up cleanup.
- 2026-06-22: **C3 — AI top-up pack / overage purchase (Phase D, billing loop complete).** One-time Stripe Checkout session (`mode: 'payment'`) lets an org owner buy a pack of bonus AI sessions that do not reset monthly and are consumed only after the included allowance is exhausted. **What shipped:** (1) `Organization.aiBonusSessions` field — both in-memory and Table Storage twins updated; (2) `POST /api/billing/topup` (owner-only) creates a `mode: 'payment'` Checkout with `metadata.kind = 'ai_topup'`; the existing webhook handler branches on that kind and credits `aiBonusSessions` via `updateOrganization`; (3) `GET /api/billing/status` now returns `topupAvailable` + `topupPackSize`; (4) `POST /api/ai/speech/token` allowance gate extended to total = `aiIncludedSessions + aiBonusSessions`, deducting from bonus before issuing the token when monthly is exhausted; (5) `GET /api/ai/usage` returns `bonus` alongside `included`/`remaining`; (6) Frontend: `AiUsage.bonus` and `BillingStatus.topupAvailable/topupPackSize` added; `OrganizationPage` shows bonus-session count in the meter label, a "Buy N more sessions" button when low/exhausted and owner+topup is available, and handles `?topup=success`/`?topup=cancelled` redirect params. New env vars: `STRIPE_PRICE_AI_TOPUP` (required), `AI_TOPUP_PACK_SIZE` (optional, default 25). `api_register.json` → added `POST /api/billing/topup`, updated `GET /api/ai/usage` schema. *Backend 680 tests pass; frontend typecheck clean.*
- 2026-06-22: **UAT minor polish — app-picker card gating, truck-check history honesty, join-check resume.** Three small UAT findings cleared together. **(1) App-picker:** the Sign-In, Vehicle Check, and Reports cards always rendered as live links regardless of plan, while AAR Studio + suite cards showed a "Not in your plan" lock; the three core cards now use the same `feature-card--locked` + upgrade-link treatment when the org lacks the feature (unchanged with no entitlement context — single-tenant/kiosk back-compat). New `LandingPage` test for the locked affordance. **(2) Truck-check history:** the workflow's "Cancel" only navigates away, leaving the run `in-progress`; the admin Check History labelled every run "Completed by …", so an abandoned check read as completed — in-progress runs now get an amber "In progress" badge + "Started by" label. **(3) Join Check:** joining already loaded the crew's existing results, but the joiner landed on item 0; it now advances to the first uncompleted item so a shared check visibly resumes. Frontend 453 tests pass; typecheck + lint clean.
- 2026-06-22: **AAR Studio — close the direct-nav entitlement bypass (UAT should-fix).** AAR Studio (`/aar`) had no plan check on load, so a signed-in user on a non-AI-plan org could reach the full app by direct navigation; its cloud-sync (`/api/aar-sessions`) and AI (`/api/ai/*`) calls then silently 403/503 — already gated server-side by `requireFeature('aarStudioEnabled')`, but the UI looked fully functional. **What shipped:** new `aar-studio/js/lib/entitlement.js` (pure, injectable fetch/token, node-tested) exposing `fetchEntitlements`/`decideAccess`/`checkAccess`; `main.js` now `boot()`s behind it. The gate is **fail-open** — a signed-out visitor (no `auth_token`, so no probe at all), an org with null entitlements, or any transient probe failure all proceed (preserving the documented local-first / signed-out workflow); the only blocking outcome is a positive `aarStudioEnabled === false` on a signed-in org, which renders an upgrade screen (links to `/admin/organization` and the suite home) instead of the app. Same-origin probe, covered by the existing `/aar` CSP `connect-src 'self'`. 13 new tests (`test/entitlement.test.js`); AAR Studio suite 85 pass. Updated `aar-studio/docs/ARCHITECTURE.md` (lib listing + a Security section note). **The backend `requireFeature` remains the security boundary; this is the matching client-side UX gate.**
- 2026-06-22: **Security — close anonymous data-exposure on read endpoints (top UAT blocker).** The single highest-priority UAT finding: `GET /api/reports/*`, `GET /api/truck-checks/appliances` (+ other truck-check reads), and `GET /api/members` returned real attendance/compliance analytics and member PII to fully credential-less requests. Root cause: `reports`/`truck-checks` had **no** auth middleware (only `stationMiddleware`), and `members`' `flexibleAuth` was dormant (gated behind the unset `ENABLE_DATA_PROTECTION` env, and its scope check only accepted `role==='admin'` so it would have 403'd owners anyway). **What shipped:** (1) new always-on `requireSession({ readsOnly })` middleware (`backend/src/middleware/flexibleAuth.ts`) — allows a request through on a valid JWT (**any** role: owner/admin/viewer), a valid brigade/kiosk token (`X-Brigade-Token`), or the public demo station; otherwise **401**. `readsOnly` only gates GET/HEAD so write-path kiosk actions (check-ins, truck-check results) keep their existing pass-through, exactly as UAT recommended. (2) Mounted it in `index.ts` on the `/api/members`, `/api/truck-checks`, and `/api/reports` routers (before `requireFeature`). (3) Frontend `services/api.ts` now forwards the kiosk brigade token as `X-Brigade-Token` on every request when in kiosk mode — the `kioskMode` util always claimed "the brigade token is included in all API requests" but `getHeaders()` never actually did it, so logged-out kiosks were authenticating as *nobody*; this makes real (non-demo) kiosks keep working under the new gate. **Why mount-level:** existing route unit tests mount routers directly (not via `index.ts`), so the gate protects production without breaking them. 9 new tests (`dataAccessProtection.test.ts`: anon→401, JWT owner/viewer→200, bad JWT→401, valid/invalid brigade token, demo bypass, write pass-through). Backend 680 pass; frontend lint clean + 452 pass; both typecheck clean. **Follow-up:** `api_register.json`/`API_DOCUMENTATION.md` should note the new 401 on these reads; member/truck-check *write* endpoints remain anonymous (out of UAT scope, separate hardening).
- 2026-06-22: **Production UAT pass — full walkthrough, new signup to logged-out API probe.** Hands-on UAT against the live `bungrfs-linux.azurewebsites.net` deployment via browser automation; full results in `docs/current_state/UAT_REVIEW_20260622.md`.

  **Blockers found:**
  - **Anonymous data exposure (security):** `GET /api/reports/*`, `GET /api/truck-checks/appliances`, and `GET /api/members` all return real organizational data (attendance, member-participation, truck-check compliance %, real member names) to fully credential-less requests — verified with `fetch(url, {credentials:'omit'})` from a brand-new browser tab with no cookie/token/station context. By contrast `/api/auth/me`, `/api/organizations/current`, and `/api/billing/status` correctly 401 the same request, so the auth layer works generally — it's specifically missing on these read routes. CLAUDE.md's documented kiosk/demo pass-through ("requests with no org context always pass through") is reasonable for sign-in/truck-check *write* actions at a station kiosk; it should not extend to analytics/PII *read* routes. **This is now the single highest-priority fix in this document, ahead of Stripe.**
  - **Truck Check has no working configuration.** Seeded demo appliances ship with no Vehicle Type/template, so running a check on any of them skips to "0 of 0 items completed (NaN%)" — a fake 100%-complete result with an empty checklist (reproduced on 3 of the 5 seeded appliances). Linking a vehicle to a Vehicle Type fixes the empty-checklist problem but then `POST /api/truck-checks/results` 400s on the very first item (reproduced 3×, both Done and Skip) — so no path currently completes a check successfully. Truck Check is marketed as shipped in v1.1; it does not function on production today.
  - **Stripe billing confirmed not configured in production** (both "Upgrade to Basic" and "Upgrade to AI Pro" return "Billing is not configured on this server") — no path exists for any org to leave the free Community plan. This was already tracked as a roadmap item (C2 below); UAT confirms it's blocking real users today, not just a backlog gap. Also confirms `README.md`'s "Stripe billing wired in test mode" claim is inaccurate — matches CLAUDE.md, not the README.

  **Should-fix found:** AAR Studio (`/aar/`) has no entitlement check on load and is fully reachable by direct navigation on a Community-plan org with `aiEnabled` off (the app-picker's locked card is cosmetic only, since AAR Studio sits outside the SPA router). The `/admin/organization` page's 5-requests/15-minute rate limiter fires after ~5 normal page loads in 4 minutes, locking a brand-new owner out of their own settings for up to 15 minutes with no retry-after messaging. Toggling a module past its plan ceiling shows a green "Updated" success banner while silently reverting the checkbox — the server-side clamp is correct, the success messaging is not. "Add New Vehicle" past the 5th appliance on Community 403s with no error toast (infinite spinner instead). "Template" on any vehicle without a saved per-vehicle override hangs the dashboard indefinitely (404 treated as a thrown error instead of "use Vehicle Type defaults").

  **Confirmed-live (already-tracked) gaps:** `maxStations` is unenforced — created a 2nd station on Community with no ceiling/warning (matches existing C1 item below).

  **Minor:** ~~App-picker's Reports card looks active when off-plan (other locked cards show a pill instead)~~ **Fixed 2026-06-22** — Sign-In, Vehicle Check, and Reports cards now use the same entitlement-locked treatment as AAR Studio / suite apps (`feature-card--locked` + a "Not in your plan" → `/admin/organization` link) when the org lacks the feature; unchanged with no entitlement context (back-compat). Remaining minor: double PWA service-worker registration log; `/login` and `/profile/:memberId` take 3.5–5s to settle vs near-instant elsewhere; ~~Cancel leaves a phantom "completed" history entry~~ **Fixed 2026-06-22** — the truck-check workflow's "Cancel" only navigates away, leaving the run `in-progress`; the admin Check History showed every run as "Completed by …" regardless. The history now badges `in-progress` runs ("In progress", amber, with a left border) and labels them "Started by" rather than "Completed by", so an abandoned check no longer masquerades as completed. ~~Join Check doesn't resume shared progress~~ **Improved 2026-06-22** — the join flow already loaded the crew's existing results (completed items render as done in the sidebar), but the joiner always landed on item 0 so it felt like a fresh start; joining now advances `currentIndex` to the first item the crew hasn't completed yet and scrolls to it, so a shared check visibly picks up where it was left off.

  **Not completed:** re-login smoke test at the end of the session (test password not retained across a context boundary — a testing-process gap, not a product finding). Screenshots were captured live during the session but were not placed into `docs/current_state/images/` — the capture tool's save target wasn't reachable from the environment this review was written in; revisit once there's a path to move them into the repo.

  *No code changed — this is a UAT/findings-logging session only.*

- 2026-06-22: **UAT re-test (PM) — Stripe live; AAR Studio AI-gateway gap found.** Owner set `STRIPE_SECRET_KEY`/`STRIPE_WEBHOOK_SECRET`/`STRIPE_PRICE_*` in App Service; re-verified billing end-to-end and ran a deeper AAR Studio pass against a second test org. Full detail in the "Re-test — June 22, 2026 (PM)" section of `docs/current_state/UAT_REVIEW_20260622.md`.

  **Resolved:** Stripe checkout now works in production — a real Checkout session completed in test mode, landing the org on plan `ai`, status `trialing`, 14-day trial, AI quota visible. Closes the billing blocker from the AM pass.

  **New blocker:** `POST /api/ai/chat` returns 503 ("The AI service isn't set up on the server yet") on a fully-entitled AI-plan org — confirmed by pasting a transcript into a real AAR review (parsed correctly, AI summarisation failed) and reproduced via manual retry. Root cause is the `AZURE_OPENAI_*`/`AZURE_SPEECH_*` env vars `aiGateway.ts` reads not being set in App Service — independent of the now-working Stripe/entitlement wiring. AAR Studio's core AI features don't function on production today even when billing and entitlements are correct.

  **Re-confirmed, unfixed:** AAR Studio still makes no `GET /api/auth/entitlements` call on load (the direct-nav plan-gate bypass from the AM pass is still live); the `/admin` 429 rate-limiting recurred, now also on a plain page load.

  **Process note:** AAR Studio's built-in "See an example" demo (a static, hardcoded "Wamboin Structure Fire" review) was loaded during this pass to sanity-check the Findings board/Report UI render well once content exists. That content is canned and unrelated to the AI gateway — flagged explicitly so it isn't mistaken for live AI output. It persisted as a second test-data record in the org; harmless but worth the owner clearing both test reviews from "Bushie UAT Brigade 2" when done. Also spotted a minor cosmetic bug: two example findings with no quote render the literal string `null` instead of a blank line.

  *No code changed — findings-logging only.*

- 2026-06-22: **Admin UX rough edges — rate-limiter, module-toggle clamp UX, vehicle-create error, template 404 fallback.** Batch fix for the four admin-area UX issues found in UAT. **What shipped:** (1) `GET /api/organizations/current` and `GET /current/users` moved off `sensitiveActionRateLimiter` (5 req/5 min) onto the outer `apiRateLimiter` already applied at the mount level in `index.ts`; only the mutating PUT and POST routes keep the tighter limit. (2) `toggleModule()` in `OrganizationPage.tsx` now compares the server-returned entitlement value to the desired value after the PUT; if `clampEntitlements` silently reverted it, a red "not available on this plan" error is shown instead of a false "Updated" banner. (3) `handleSaveVehicle()` replaced `alert()` with an inline `vehicleModalError` state displayed inside the modal — no more invisible spinner when the plan ceiling 403s. (4) `api.getTemplate()` returns `null` on 404; `handleEditTemplate()` falls back to an empty custom-items template so the editor opens normally — no more hang on vehicles with no saved override. *5 files changed.*

- 2026-06-22: **TC polish — UX integration review (closes out the TC feature track).** Systematic pass to eliminate rough edges across the full truck-check feature surface. **What shipped:** (1) *Resolution modal:* replaced both `window.prompt()` calls in the issue Follow-ups tab with an inline modal (`resolve-modal`) — resolvedBy name + resolution note fields, cancel/submit, Escape to dismiss, saving state, backdrop-click-to-close; (2) *VehicleTypesPage backdrop close:* modal overlay gained the same `role="button"` + `onKeyDown`/`onClick` pattern as VehicleManagement so Escape and backdrop-click both close the dialog; (3) *Template editor — locked standard items:* when a vehicle is linked to a VehicleType, `handleEditTemplate` now also calls `getEffectiveChecklist()` and extracts items where `isStandard === true`; the modal renders them as a read-only section ("🔒 Standard items — inherited from vehicle type") above the editable custom-items section so admins can see what's locked without needing to navigate to the Vehicle Types page; (4) *Vehicle identity on cards:* `VehicleManagement` admin cards and `TruckCheckPage` hub cards now display a subtitle line combining agencyId / make-model-year / registration where set; (5) *Open issues badge on hub cards:* `TruckCheckPage.loadAppliances()` now fetches outstanding issues in parallel and renders a "⚠ Open issues" badge (red, top-left) on any appliance card that has unresolved issues — and a subtle red border on the card itself; (6) *AAR cloud-review loading state:* `openRemote()` now adds `review-card--loading` (opacity + pointer-events-none) to the card while `fetchServerSession` is in flight, removing it on error so the user gets clear feedback instead of an unresponsive tap. All lint/typecheck/452-frontend/671-backend tests green.
- 2026-06-22: **TC-3/TC-4 — Truck-check member attribution + issue follow-up lifecycle (completes the TC uplift).** **TC-3:** the check-start screen now offers a roster typeahead (from `/api/members`) so a run is attributed to a `memberId` when the inspector is on the roster, with a free-text fallback for kiosks/non-members; `completedByName` keeps the display name in both cases (no per-item display regression). **TC-4:** `CheckResult` gained an issue lifecycle (`issueStatus` open→acknowledged→resolved, plus `issueNote`/`assignedTo`/`resolvedBy`/`resolvedAt`); recording an `issue` defaults to `open`. New `PATCH /api/truck-checks/results/:id/issue` (runId in body so the Table twin can point-update its run-partitioned result) and a station-scoped `GET /api/truck-checks/issues` flat feed enriched with appliance/run context (defaults to outstanding; `?status=` filter). A new **"Follow-ups" tab** in the admin dashboard lists outstanding issues with acknowledge/resolve/assign actions and emits a `truck-check-update` (`issue-updated`) socket event; admin-gated, no separate equipment-officer role per the owner's call. Both DB twins updated (in-memory + Table Storage). 6 new backend tests (issue lifecycle + member attribution paths); backend 671 pass, coverage over gate; frontend lint/typecheck/452 tests green. `api_register.json` → v1.8.0. **This closes the truck-check rebuild (TC-1…TC-5).** Remaining polish: a dedicated locked-standard + interleave overlay editor (custom items currently append after standard ones).
- 2026-06-21: **TC-1 — Truck-check vehicle types + locked standard checklists + vehicle identity.** The core of the truck-check rebuild. New first-class `VehicleType` (org-scoped, `isStandard` publishes a shared standard; `standardItems` are immutable for adopters and carry stable `itemCode`s for cross-brigade comparison) with in-memory + Table Storage twins (`vehicleTypeDatabase`/`tableStorageVehicleTypeDatabase`/`vehicleTypeDbFactory`, init in `index.ts`). Appliances gained `vehicleTypeId` + identity fields (`agencyId` fleet no., `registration`, `vin`, `make`, `model`, `year`) — carried via an `ApplianceDetails` object so positional create/update stayed back-compatible. New routes: vehicle-type CRUD (admin, org-owned edits; standards listed to all) and `GET/PUT /api/truck-checks/appliances/:id/checklist` computing the **effective checklist** (locked standard items live from the type — so edits propagate — merged with the brigade's custom overlay in saved order; standard items can never be dropped). Frontend: identity fields + a real standard-type `<select>` on the vehicle form, a Vehicle Types authoring page (`/truckcheck/vehicle-types`, add/reorder/remove standard checks + publish toggle), and the check workflow now consumes the effective checklist. Also fixed a CodeQL ReDoS in `slugify` (shared `utils/slug.ts`, split-based, linear) and restored backend coverage over the 73/64 gate. Backend 667 pass; frontend lint/typecheck/452 tests green; `api_register.json` → v1.7.0 (vehicle-types + checklist endpoints, `VehicleType`/`EffectiveChecklist`/`ChecklistItem` defs). **Follow-ups:** TC-3 roster-member attribution and TC-4 issue lifecycle (both owner-approved); the per-vehicle overlay editor still uses the legacy template modal (custom items append after standard) pending a dedicated locked-standard + interleave UI.
- 2026-06-21: **TC-2/TC-5 — Truck-check station isolation + stable template item ids.** First slice of the truck-check rebuild (full review recorded in the "TC — Truck Check feature" section above). **Station isolation was broken:** the route computed `stationId` and passed it, but **both** DB twins dropped it on every read — so every brigade saw every other's vehicles, runs and issues, and the Table twin never even persisted `stationId` on check runs. Made `getAllAppliances`/`getAllCheckRuns`/`getCheckRunsByDateRange`/`getRunsWithIssues`/`getAllRunsWithResults`/`getTruckCheckCompliance` honour `stationId` in both twins (via a shared `getEffectiveStationId` match, so existing single-station/undefined-station data still resolves to the default station — back-compatible), and persisted `stationId` on Table-twin check runs. **TC-5:** template edits now preserve item ids (match by supplied id, else by name) instead of regenerating a uuid per item per save, which had been breaking `itemCode`/history continuity. 3 new route tests (station isolation ×2, id stability ×1); backend 642 pass, typecheck clean. **Next (TC-1):** first-class `VehicleType` with a locked standard checklist + brigade custom items, per-vehicle agency id, and clone-to-customise.
- 2026-06-21: **A2 (increment 2, complete) — AAR sync conflict handling + CSP-shrink resolution.** Closed out A2 increment 2. **(1) Conflict handling:** the cloud backup was last-write-wins, so a second device could silently clobber a teammate's newer edit. Added timestamp-based **optimistic concurrency** end-to-end: `AARSession` gained `clientUpdatedAt` (the saving device's edit time), the route + both DB twins reject a stale save with **409** (`AarSessionConflictError`, returning the current row) when an incoming `clientUpdatedAt` is older than the stored one (omitting it keeps last-write-wins for back-compat). On the client, `serverSync.pushSession` sends `clientUpdatedAt` and flags a 409 as `conflict` (best-effort backup never clobbers); the home view now reconciles on the **read** side too — a local review whose cloud copy is newer (`isRemoteNewer`) gets a "☁ Newer version from your team" flag and a "Load latest" action (`openRemote` → `adoptSession`). 6 new tests (3 backend conflict + 3 AAR serverSync); backend 639 pass, AAR 72 pass; `api_register.json` → v1.6.1. **(2) `/aar` CSP shrink — resolved as not-yet-actionable:** the original aim was to drop the direct Azure OpenAI/Speech `connect-src` hosts once the gateway fronts all provider calls. Verified it can't be done yet — the Azure **Speech SDK still connects browser-direct** to `*.stt.speech.microsoft.com`/`*.cognitiveservices.azure.com` (the gateway only vends a short-lived token; the audio stream is browser→Azure), and BYO-Azure direct mode (`*.openai.azure.com`) is still supported in Settings. Removing those hosts would break live transcription. Left the CSP unchanged; the shrink is blocked on a streaming-audio gateway (roadmap D3/voice agent) and re-filed there.
- 2026-06-21: **A2 (increment 2, part 1) — AAR setup roster typeaheads.** Now that AAR Studio is org-identified (inc 1), the free-text setup fields are backed by the Station Manager roster. New `aar-studio/js/lib/roster.js` (`fetchStations`/`fetchMembers`/`loadRoster`) reads `/api/stations` and `/api/members` with the same-origin SM JWT; `views/setup.js` fills two `<datalist>`s so the **meeting/incident Location** fields autocomplete from station names and the **Facilitator** field from member names (de-duplicated across the org's stations, capped at 8 to bound the fan-out). Implemented as typeaheads, not hard `<select>`s, deliberately — this keeps AAR local-first and fully usable signed-out (no roster → plain text, exactly as before) and lets facilitators still type a name that isn't on the roster. No CSP change (`/api/*` is same-origin, covered by `connect-src 'self'`). 5 new AAR tests; AAR `node --test` 69 pass. **Still deferred (A2 inc 2):** bidirectional sync conflict handling (currently last-write-wins) and shrinking the `/aar` CSP once the AI gateway fronts all provider calls.
- 2026-06-21: **A2 (increment 1) — AAR Studio server-side persistence + brigade-shared reviews.** AAR reviews were trapped in one facilitator's `localStorage` — lost with the device and invisible to the rest of the brigade. Added org-scoped server persistence following the established DB-twin pattern. **Backend:** new `AARSession` type; `services/aarSessionDatabase.ts` (in-memory + `IAarSessionDatabase`, keyed by `org/id` so two orgs sharing a client id never collide), `services/tableStorageAarSessionDatabase.ts` (Table Storage twin, partition by org; payload **chunked** across `payload0..payloadN` to clear Table Storage's 64 KiB-per-property / ~1 MiB-per-entity limits), `services/aarSessionDbFactory.ts`, init wired into `index.ts`. New `routes/aarSessions.ts` — `GET/PUT/DELETE /api/aar-sessions[/:id]`, JWT-required and org-scoped, mounted behind `requireFeature('aarStudioEnabled')` (AI-plan only); payload capped at ~900k chars (413). **Identity:** reused the SM JWT AAR already reads from same-origin `auth_token` (no new login flow needed). **AAR app:** new `js/lib/serverSync.js` (best-effort `pushSession`/`list`/`fetch`/`deleteServerSession`); `store.js` debounce-backs-up on every save and mirrors deletes when signed in; the home view now shows a "From your team" section of cloud reviews not yet on this device and pulls them down on tap (`store.adoptSession`). Local-first preserved — signed-out AAR works exactly as before. 16 new tests (6 backend route + unit DB, 10 AAR `serverSync`); backend 636 pass, AAR `node --test` 64 pass. `api_register.json` → v1.6.0 with the `aar-sessions` group + `AARSession` definition. **Deferred (A2 increment 2):** station/member roster picker to replace the free-text setup screen, full bidirectional conflict handling, and shrinking the `/aar` CSP once the AI gateway fronts all provider calls.
- 2026-06-21: **Close out commercialisation track + fix broken page scroll.** Two threads. **(1) Plan reconciliation + billing audit persistence:** discovered C1 (#552) and C2 (#553) had shipped and merged to `main` but were never recorded here — the #575 plan-collapse listed them as still-to-do. Recorded both as shipped and hardened the billing audit trail: the Stripe webhook log was an in-memory array (`billingEventLog` in `routes/billing.ts`) that vanished on restart and gave no idempotency. Added `BillingEvent` persistence following the `UsageRecord` twin pattern — `services/billingEventDatabase.ts` (in-memory + `IBillingEventDatabase`), `services/tableStorageBillingEventDatabase.ts` (Table Storage twin, partition by org), `services/billingEventDbFactory.ts`, wired `initializeBillingEventDatabase()` into the startup sequence in `index.ts`. The webhook now (a) skips reprocessing an already-recorded event id (idempotency) and (b) persists every event via the factory; added an owner-only `GET /api/billing/events` audit endpoint. Registered the billing **and** AI gateway endpoints in `api_register.json` (v1.5.0) — the previously-noted docs follow-up — plus a `BillingEvent` definition. 11 new backend tests (`billing.test.ts` + `billingEventDatabase.test.ts`); backend 623 pass. **(2) Scroll bug:** desktop `body`/`#root` were locked to `height:100vh; overflow:hidden` (kiosk shell model) while the document-flow content pages (marketing, signup/login, organization) lay out with `min-height:100vh` and no inner scroll container — so on desktop their content was clipped and the page could only be moved with the keyboard (focus-into-view). Mobile already relaxed this; desktop didn't. Relaxed the base `body`/`#root` rules to `min-height:100vh` + `overflow-x:hidden` (vertical document scroll allowed); the self-contained 100vh kiosk pages (`.app`, `.landing-page`) are unaffected (exactly viewport height → no document scrollbar). *Frontend lint, typecheck, and 452 Vitest tests green.*
- 2026-06-20: **C2 — Stripe billing, SaaS Phase B (#553).** Wired live billing on top of the SaaS tenant model. New `services/stripeClient.ts` (lazy `stripe` SDK init from `STRIPE_SECRET_KEY`, `isStripeConfigured()`, `resolvePriceId(plan, interval)` from `STRIPE_PRICE_*` env vars) and `routes/billing.ts`: owner-only `POST /api/billing/checkout` (Checkout session, 14-day trial), `POST /api/billing/portal` (Customer Portal), `GET /api/billing/status`, and a signature-verified `POST /api/billing/webhook` (raw-body, registered before `express.json()`) that syncs org `planCode`/`status`/`entitlements` on `checkout.session.completed`, `customer.subscription.updated/deleted`, `invoice.paid`, and `invoice.payment_failed`. `Organization` gained `stripeCustomerId`/`stripeSubscriptionId`/`trialEndsAt`/`billingEmail`; `BillingEvent` type added. SPA: `/admin/organization` upgrade UI + `TrialBanner`. Endpoints return 503 when Stripe isn't configured, so non-billing deployments are unaffected. *(Audit log was in-memory in this slice — persisted to Table Storage on Jun 21.)*
- 2026-06-20: **C1 — Entitlement-enforcement hardening (#552).** Flipped `ENABLE_ENTITLEMENTS` to default-on (opt-out `=false` for dev). Added a soft JWT decode in `attachOrganization` so routes without explicit `authMiddleware` (e.g. `/api/export`) still resolve org context; gated `GET /api/export` behind `requireFeature('reportsEnabled')`; added `enforceStationLimit()` on `POST /api/stations` (enforces plan `maxStations`, excludes system stations from the quota). 8 new integration tests covering every gating path; fixed a missing `@testing-library/dom` peer dep broken by prior dependabot bumps.
- 2026-06-21: **UI/UX polish — CSS-var sweep, dark-mode fixes, status tokens, entitlement UX (#576).** A systematic pass to harden the frontend design system and eliminate silent style regressions.

  **What shipped:**
  - **`TrialBanner` wired into the shell** (`App.tsx`): the component existed with full business logic (billing status fetch, trial countdown) but was rendered nowhere — trial-plan users never saw their status. Fixed by inserting `<TrialBanner />` into the app shell.
  - **Dead-end upgrade path fixed** (`LandingPage.tsx`): locked feature cards (AAR Studio, suite siblings) were non-interactive `<span>` elements. Converted to `<Link to="/admin/organization">` so the "Upgrade" CTA actually navigates. Updated `.feature-link--locked` CSS to `cursor: pointer` + hover state (`--surface-warning` background, amber border).
  - **`FeatureRoute` rewrite**: replaced inline styles with `FeatureRoute.css` (design-system vars throughout). Changed copy from generic "isn't enabled" → "isn't on your plan" with a styled upgrade CTA ("View upgrade options →" → `/admin/organization`, "Back to home" → `/`).
  - **Dark-mode pattern fix** (`LoginPage.css`): was using `@media (prefers-color-scheme: dark)` — the in-app theme toggle sets `[data-theme="dark"]` on `<html>`, not the OS preference. Rewrote all dark-mode blocks as attribute selectors.
  - **Semantic status tokens** (`index.css`): added `--status-{done,issue,skip}-{border,bg,text}` in both `:root` (light) and `[data-theme="dark"]` — truck check inspection states now adapt to theme.
  - **Undefined CSS-var sweep**: replaced all undefined vars across 10+ files:
    - `--card-bg`/`--input-bg`/`--hover-bg` → `--bg-card`/`--bg-secondary`/`--surface-hover` (ReportsPage, DataTable, ExportMenu, InsightCard, StationPulse)
    - `--color-border`/`--color-surface`/`--color-text`/`--color-text-secondary`/`--color-error`/`--color-primary` → design-system equivalents (UpgradePrompt)
    - `LoginPage.css` error/focus colors → `--surface-error`/`--text-error-strong`/`--rfs-core-red-light`/`--rfs-core-red-dark`
  - **Material Design colors isolated from truck check**: `AdminDashboard.css`, `CheckSummary.css`, `CheckWorkflow.css`, `TruckCheckPage.css` all used hardcoded Material greens/ambers/greys (`#4caf50`/`#ff9800`/`#757575`). Replaced with `--status-*` tokens so truck-check states now respond to dark mode.
  - **OrganizationPage semantic colors**: success/error messages, plan badges, trial notices, AI-nudge all use `--surface-*`/`--text-*-strong`/`--ui-amber` tokens instead of hardcoded hex.
  - **TrialBanner.css** fixed from hardcoded `#fff3cd`/`#856404`/`#ffc107` → `--surface-warning`/`--text-warning-strong`/`--ui-amber`.
  - **Test updated**: `FeatureRoute.test.tsx` assertions updated to match new copy; file rewritten to avoid Unicode curly-quote parser rejection.

  *3 commits; frontend lint, typecheck, and Vitest all green.*

- 2026-06-20: **Bushie Tools suite — Phase 1: shared identity & subscription (#556).** Stood up Station Manager's entitlements as the canonical suite licensing layer (Option A / federation — apps stay in their own repos and validate the same SM JWT). **Per-app flags:** `Entitlements` (and the frontend mirror) gained `aarStudioEnabled` (true on AI Pro), `santaRunEnabled` and `fireBreakEnabled` (reserved — no plan grants them yet); `constants/plans.ts` sets them per tier and `clampEntitlements()` enforces the ceiling. **Cross-app endpoint:** new `GET /api/auth/entitlements` — a lightweight JWT-verified probe returning `{ entitlements, planCode, status }` (null org → null payload) so sibling apps gate features without the larger `/auth/me` shape; same-origin in Phase 1, cross-origin covered by the existing `FRONTEND_URLS`/`allowedOriginsList` CORS seam. **Auth decision:** kept SM JWT as the suite IdP rather than Entra External ID — kiosk/station iPads authenticate with brigade access tokens (not user accounts), a scenario the JWT/brigade-token model already supports; recorded in the new protocol doc. **App-launcher:** the logged-in `LandingPage` now doubles as the suite launcher — the AAR Studio card locks to an "Upgrade to AI Pro" placeholder below the AI plan, and new sibling-app cards (Fire Santa Run — seasonal-badged; Fire Break Calculator) render from a `config/suiteApps.ts` catalog, gated by their flags and linked out via build-time `VITE_SANTA_RUN_URL`/`VITE_FIREBREAK_URL` (unlocked when no org context, for back-compat). New `docs/SUITE_TOKEN_VALIDATION.md` documents the validation contract for sibling apps. Shipped in two slices: entitlements + endpoint (4 backend tests, merged in #571) then launcher + docs (2 frontend tests); backend (608) + frontend (452) + AAR (54) suites, lint, and both typechecks green. `api_register.json` bumped to 1.4.0 with the new endpoint. **Deferred to Phase 2/3:** SSO redirect flow for cross-origin apps, shared `@rfs/*` packages, and coordinating the Santa Run / Fire Break repos to consume the endpoint.
- 2026-06-20: **AAR Studio collaborative session notes — core slice (#551).** A whole room can now contribute to a live After Action Review. The facilitator hosts a session (a deterministic, legible join code derived from the session id, e.g. `WAMB-42`) and shares a join link; participants open it on their phones (no account/install) and push timestamped free-text notes that fan out to the facilitator's board. **Transport:** reuses the existing Socket.io server (confirmed with the owner over SignalR/WebRTC) — new `backend/src/services/aarCollab.ts` registers ephemeral `aar:host`/`aar:join`/`aar:note` room handlers (room = `aar-<CODE>`); the relay is stateless (the facilitator's browser is the source of truth and stores notes in its localStorage session). Validation: code format, note/label length caps, per-socket throttle. The `/aar` scoped CSP gained `ws:`/`wss:` (same-origin Socket.io); socket.io-client loads from jsDelivr. **AAR app:** new `js/lib/collab.js` (socket wrapper + code/URL helpers), `js/views/join.js` (minimal participant "add a note" page routed at `#/join/<CODE>`, nav hidden), a collapsible **Room notes** panel on the capture screen, and a `notes[]` stream on the session model. **Findings integration:** un-analysed notes are fed into the AI extraction pass as clearly-attributed evidence (shaped like transcript segments) and marked analysed alongside segments — joining needs no AI gateway; only the facilitator's analysis does (already AI-gated). 11 backend + 6 AAR tests; backend coverage 75.4% (aarCollab.ts ~97%), AAR `node --test` 54 pass. **Follow-ups (deferred from the core slice):** offline buffer + resync, note→finding promotion, richer participant presence/labels. iPad screenshots pending manual capture.
- 2026-06-20: **Bushie Tools rebrand + design-system unification (#549 + #550).** Shipped together per the roadmap. **Shared tokens:** new `aar-studio/css/rfs-tokens.css` is the canonical brand source of truth (RFS red `#e5281B`, lime `#cbdb2a`, neutrals, Public Sans, spacing/radius scales, `--tap-min: 60px` / `--tap-min-sm: 44px`); the SPA's `frontend/src/index.css` mirrors the same values. **AAR Studio realigned:** `css/app.css` now maps its semantic aliases (`--red`/`--gold`/etc.) onto the `--rfs-*` tokens (killing the old `#c8102e`/gold drift), the topbar is RFS red with corrected nav contrast, the body font is Public Sans (loaded via Google Fonts — the `/aar` scoped CSP gained `fonts.googleapis.com`/`fonts.gstatic.com`), and sub-44px controls were bumped (`.btn`/inputs/chips/icon-buttons ≥44px WCAG 2.1 AA, primary actions/`.btn--big`/`.phase-btn` ≥60px kiosk). **Rebrand sweep:** AAR title bar + `<title>` now read "AAR Studio · Bushie Tools" with a suite link home; the SPA app-picker header, `frontend/index.html` meta/OG/Twitter, the PWA manifest (`public/manifest.json` + Vite `manifest`), and the root/AAR READMEs adopt the Bushie Tools suite identity (the marketing page already did in #554). No SPA visual regression (tokens unchanged there); the landing test was updated to the new brand string; frontend lint/typecheck/tests and AAR `node --test` green. **Interpretation note:** "≥60px touch targets" was applied as 60px for primary actions and 44px (WCAG AA) for inline/nav controls to avoid a janky 60px-tall header — flagged on the PR for the owner to tighten to strict-60 if preferred. iPad screenshots pending manual capture.
- 2026-06-20: **Server-side AI gateway (#555).** Added `/api/ai/*` so AI provider credentials live server-side and AAR Studio no longer calls Azure browser-direct with user-supplied keys. New backend pieces: `services/aiGateway.ts` (provider adapters — Azure OpenAI chat/report, Azure Speech token vend, Anthropic stub — all env-driven via `AZURE_OPENAI_*`/`AZURE_SPEECH_*`); `routes/ai.ts` with `POST /api/ai/chat`, `POST /api/ai/report`, `POST /api/ai/speech/token`, `GET /api/ai/usage`; a `UsageRecord` entity with in-memory + Table Storage twins + factory (`usageDatabase.ts`/`tableStorageUsageDatabase.ts`/`usageDbFactory.ts`); and `services/meteredUsageReporter.ts` (hourly, opt-in Stripe metered reporting — safe no-op until a meter is configured). Gating: `aiEnabled` entitlement required when org context is present; the monthly allowance (`aiIncludedSessions`) is measured in **sessions** (one speech-token vend == one session) and enforced at the speech-token endpoint with a 402 `{ remaining, resetAt }`; chat/report need `aiEnabled` but aren't hard-gated so in-flight analysis is never cut off; no org context passes through (consistent with existing entitlement back-compat). **AAR Studio migration:** `lib/llm.js` gains gateway mode (default when no user endpoint is set) posting to `/api/ai/chat`|`/report` with a Bearer token read from the same-origin SPA's `auth_token`; `audio/speech.js`/`audio/live.js` use `SpeechConfig.fromAuthorizationToken` with a token vended from `/api/ai/speech/token` (402/403/503 surface friendly upgrade/sign-in messages); the Settings screen is repositioned as an optional local/dev BYO-Azure override. **SPA:** `/admin/organization` shows an AI sessions-used-this-month meter with a low-allowance nudge (`GET /api/ai/usage`). 22 backend + 5 AAR gateway tests; backend/frontend coverage and AAR `node --test` all green. This reverses the earlier "browser-direct, no backend" AAR constraint by design (confirmed with the product owner). **Note:** the `/api/ai/*` REST endpoints are not yet in `api_register.json` (matching the billing slice); registering both is a docs follow-up.
- 2026-06-20: **Marketing & pricing landing page (#554).** Added a logged-out front door for Bushie Tools at `/` (`features/marketing/MarketingPage`): hero + "for the average bushie" copy, app highlights (Station Manager, AAR Studio; Fire Break Calculator + Fire Santa Run flagged "coming soon"), a three-tier pricing table (Community free / Basic A$10/mo / AI Pro A$19/mo) with a monthly⇄annual toggle (annual = 2 months free), GST-inclusive callout, a Bushie Suite waitlist card, and an FAQ. A new `HomeRoute` decides `/`: marketing page when logged out, the app-picker (`LandingPage`, now also at `/apps`) when authenticated or in demo mode (`/?demo=true`). Paid-plan CTAs deep-link to `/signup?plan=…&interval=…`; `SignupPage` now reads those params and, after creating the org + owner, kicks off Stripe Checkout for the chosen plan (graceful fallback to `/admin/organization` when billing isn't configured). 6 new MarketingPage tests + 2 new SignupPage checkout tests; lint, typecheck, and frontend coverage all green. **Note:** `SignupPage` importing the API client pulled the large, near-0%-covered `services/api.ts` into the Vitest coverage graph — mocking the api module in `SignupPage.test.tsx` keeps it out and restores the global thresholds (a pattern to reuse when a tested component statically imports `api.ts`).
- 2026-06-06: Dependency security remediation. Cleared all open Dependabot alerts — backend 25 → 0 (3 critical, 10 high resolved) and frontend 28 → 0 (4 critical, 14 high resolved). Fixes applied via in-range `npm audit fix` (direct deps `express-rate-limit`, `multer` patched via lockfile) plus targeted `overrides` for transitive chains: backend `protobufjs ^7.5.5` (clears the OpenTelemetry/applicationinsights chain) and `@azure/functions-old → uuid ^11.1.1`; frontend `exceljs → uuid ^11.1.1`. No production code changes; backend (516) and frontend (411) test suites and builds all pass.
- 2026-06-06: Dependabot consolidated to one grouped PR per ecosystem (npm across root/backend/frontend; GitHub Actions separately) with `open-pull-requests-limit: 1` to prevent PR pileups. Added `CLAUDE.md` at repo root as an AI-agent navigation guide.
- 2026-06-06: Coverage thresholds re-enabled (backend jest.config.js + frontend vite.config.ts). Added exclusions for untestable files (Azure-credential-dependent services, browser-only APIs). Thresholds set ~2% below actual to gate on regression immediately. PR #507.
- 2026-06-06: **v1.1 shipped.** Removed ComingSoonPage gates from `/truckcheck/*` and `/reports/*`. Wired up all 9 sub-routes (6 truck check, 3 reports) as lazy-loaded components. Landing page cards activated, "Coming in v1.1" badges removed, footer version bumped to 1.1. PR #509.
- 2026-06-06: **Truck check cross-brigade schema foundation.** Added backward-compatible optional fields to enable consistency and trend analysis across brigades: `Appliance.vehicleType` (canonical type slug, e.g. `cat7-tanker`), `ChecklistItem.itemCode` (canonical item slug, e.g. `tyre-condition`) + `ChecklistItem.section` (grouping label), and `CheckResult.itemCode`/`section` (denormalised onto each result at check time so historical data stays comparable even after template edits). Threaded through shared types, both DB twins (in-memory + Table Storage), the `ITruckChecksDatabase` interface, routes, validation (slug enforcement for codes), and the frontend API client + workflow. All fields optional → existing templates/appliances/results unaffected. 9 new backend tests. UI for entering these values follows in the truck-check UX pass.
- 2026-06-06: **Truck check UX pass** (builds on the schema foundation). Editor: vehicle modal gains a canonical Vehicle Type datalist; template item editor gains Standard Item (item-code) + Section datalists and now persists those fields (previously stripped on save). Workflow: checklist renders under section headers, and a "Mark remaining as OK" fast-path completes all untouched items in one tap (issues left intact) — paper-clipboard speed. Appliance list shows a "Last checked …/Never checked" indicator so overdue vehicles stand out. New shared `checklistVocabulary` module (canonical vehicle types, item codes, sections + slug/label resolvers) with 12 unit tests. Standalone `TemplateEditorPage` left untouched (non-destructive; not linked in UI).
- 2026-06-07: **Truck check CSV export.** Added "Export CSV" button with optional date range filter directly in the Truck Check Admin Dashboard (history tab), completing Issue #12's "Truck check: Export results button" requirement. Reuses existing `api.exportTruckCheckResults()` + `downloadCSV` utilities. Also confirmed Issues #12 (CSV export + member duration), #13 (enhanced member search/filter/sort), and #6 (structured logging) are fully implemented in code — MASTER_PLAN success criteria updated to reflect current state.
- 2026-06-07: **Infrastructure-as-Code introduced (Issue #513).** Added `infra/` (Bicep) — the project's first IaC. Codifies the Table Storage data tier and provides a **dual-host comparison** (Linux B1 always-warm vs Azure Container Apps scale-to-zero) running the current Socket.io app unchanged, side-by-side with prod (prod untouched). Includes a multi-stage `Dockerfile`, a manual `infra-deploy.yml` workflow, and `infra/README.md`. **Critical finding:** the GitHub→Azure OIDC app registration was deleted from the tenant, silently breaking the last several `main` deploys (`AADSTS700016` at `Login to Azure`) — so the v1.1 merge did not reach production. The IaC README documents the one-time OIDC bootstrap that restores prod deploys. Real-time decision: **defer SignalR**; when multi-brigade scale needs a backplane, adopt **Azure Web PubSub for Socket.IO** (keeps the code, free tier) rather than a SignalR SDK rewrite. Follow-ups to raise: Windows→Linux prod migration, App Insights daily cap, managed-identity storage auth.

### February 2026 Stabilization
- 2026-02-10: Admin portal color-contrast remediation completed. Added accessible status/alert tokens (`--surface-error/warning/info/success`, `--text-error-strong`, `--text-warning-strong`, `--text-on-amber`) and applied them across admin alerts and badges. Before/after iPad screenshots captured in `docs/current_state/ADMIN_CONTRAST_REVIEW_20260210.md`.


---

## Document Version History

| Version | Date | Author | Changes |
|---------|------|--------|---------|
| 1.0 | Jan 2026 | Initial | Original master plan created |
| 2.0 | Jan 2026 | Comprehensive Review | Complete restructure: Added architecture guidance, consolidated roadmap into 4 phases (Quality → Stability → Features → Advanced), converted all backlog items into detailed issues with objectives, user stories, steps, success criteria, effort estimates, priorities, labels, and milestones. Added UI screenshot requirements. |
| 2.1 | Feb 2026 | Security Re-evaluation | Updated security considerations based on kiosk mode re-evaluation. UUID v4 brigade tokens provide sufficient security (2^128 possibilities, brute force impractical, no financial incentive). Traditional user authentication not required - would add friction without meaningful security benefit. Added 6 new ready-to-go issues from Project Review (Issues #27-32): accessibility improvements, onboarding wizard, mobile optimization, error handling, color contrast audit, and bundle optimization. |
| 2.2 | Feb 2026 | Bundle Optimization | Completed Issue #32: Bundle Size Analysis & Optimization. Achieved 45% reduction in main bundle (69.66 KB gzipped, well under 250 KB target), 99% reduction in ReportsPageEnhanced (6.37 KB gzipped), implemented vendor chunking for better caching, lazy loading for export utilities (saves 452 KB gzipped initially), and added CI/CD bundle size monitoring. |
| 2.3 | Feb 2026 | Bug Fix | Fixed participant removal failures across all UI entry points. Root cause: Table Storage implementation of `removeEventParticipant` always returned false because it lacked the partition key (eventId). Updated database interface and implementations to accept optional eventId parameter, enabling efficient single-partition deletion. Backward compatible with fallback to search. All tests pass (498 backend + 386 frontend). |
| 3.0 | Jun 2026 | Consolidation | Collapsed all five separate planning docs (CONSOLIDATION_REVIEW, SUITE_INTEGRATION_PLAN, SAAS_COMMERCIALIZATION_DESIGN, AI_MAINTENANCE_AGENT_DESIGN, AAR_STUDIO_PLAN) into this single file. Archived originals in `docs/archive/`. ONE-PLAN RULE enforced. (#575) |
| 3.1 | Jun 2026 | UI/UX polish | Recorded CSS-var sweep, dark-mode fixes, status tokens, TrialBanner wiring, FeatureRoute upgrade UX, dead-end upgrade-path fix, and Material Design color isolation in truck check. (#576). Added "Prioritised next steps" section and CLAUDE.md progress-update instruction. |
| 3.2 | Jun 2026 | Commercialisation close-out | Reconciled the plan with `main`: recorded C1 (#552) and C2 (#553) as shipped (they were merged but never logged). Persisted the Stripe `BillingEvent` audit trail to both DB twins with webhook idempotency + an owner-only `GET /api/billing/events`; registered billing & AI gateway endpoints in `api_register.json` (v1.5.0). Fixed a desktop scroll regression (document-flow content pages clipped by the kiosk `overflow:hidden` shell lock). Re-prioritised next steps (OIDC deploy restore now #1). Also logged full UAT pass against `bungrfs-linux.azurewebsites.net` with critical findings (anonymous data access to reports/members/appliances, Truck Check configuration broken, Stripe billing not configured). Documented in `docs/current_state/UAT_REVIEW_20260622.md`. |
| 3.3 | Jun 2026 | AAR persistence | A2 increment 1: org-scoped server-side persistence for AAR Studio reviews (`/api/aar-sessions` + `AARSession` DB twins, payload chunked for Table Storage) and brigade-shared "From your team" reviews in the AAR home view, reusing the same-origin SM JWT. `api_register.json` → v1.6.0. Re-scoped next steps (A2 inc 2 / T1 inc 2 mechanism caveat; OIDC de-prioritised per owner). |
| 3.4 | Jun 2026 | AAR roster typeaheads | A2 increment 2 (part 1): AAR setup Location/Facilitator fields autocomplete from the SM roster via `roster.js` + `<datalist>` (reusing `/api/stations` + `/api/members`, no new endpoints). Kept as typeaheads to preserve local-first/signed-out use. Remaining inc-2 work: bidirectional conflict handling and `/aar` CSP shrink. |
| 3.5 | Jun 2026 | AAR conflict handling | A2 increment 2 complete: timestamp-based optimistic concurrency for AAR sync (`clientUpdatedAt` + 409 across route/twins; client sends it and reconciles newer cloud copies on the home view via `isRemoteNewer`/"Load latest"). `api_register.json` → v1.6.1. `/aar` CSP shrink resolved as blocked (Speech SDK connects browser-direct), re-filed under D3. A2 done; next focus returns to T1. |
| 3.6 | Jun 2026 | Truck-check rebuild (TC-1/2/5) | End-to-end review of truck checks (findings TC-1…TC-6) + first-class `VehicleType` with locked standard checklists, per-vehicle identity (agency no./rego/VIN/make/model/year), effective-checklist resolution, vehicle-type authoring UI, station-isolation fix, stable template item ids, and a CodeQL ReDoS fix. `api_register.json` → v1.7.0. Remaining: TC-3 (member attribution), TC-4 (issue lifecycle). |
| 3.7 | Jun 2026 | Truck-check TC-3/TC-4 | Completed the TC uplift: roster-member attribution on the check-start screen, and an issue follow-up lifecycle (open→acknowledged→resolved, assignee/notes) with `GET /issues` + `PATCH /results/:id/issue` and an admin "Follow-ups" tab. `api_register.json` → v1.8.0. |
| 3.8 | Jun 2026 | TC UX polish | Integration review pass closing the TC track: inline resolution modal (replaces browser `prompt()`), VehicleTypesPage backdrop close, template editor shows locked standard items read-only, vehicle identity on hub + admin cards, open-issues badge on hub cards, AAR cloud-review loading state. TC track complete. |
| 3.9 | Jun 2026 | Deploy perf | Diagnosed the ~8-min Azure deploy as OneDeploy extracting `backend/node_modules` onto the `/home` Azure Files share; switched to `WEBSITE_RUN_FROM_PACKAGE=1` (mount, not extract) with winston logs redirected to writable `/home/LogFiles/app` and the app-setting flipped after the logger fix is deployed. (#586) |
| 4.0 | Jun 2026 | Security — anonymous data exposure | Closed the top UAT blocker: reports/members/truck-check **read** endpoints leaked real analytics + member PII to credential-less requests. Added always-on `requireSession({ readsOnly })` (JWT any-role / kiosk token / demo bypass → else 401) on the three read routers, and made the frontend actually forward the kiosk brigade token (`X-Brigade-Token`) so logged-out kiosks keep working. Write-path kiosk actions keep pass-through. 9 new tests; backend 680 pass. |
| 4.1 | Jun 2026 | Admin UX rough edges | Rate-limiter fix (org GETs off sensitiveActionRateLimiter), module-toggle plan-ceiling honest error, vehicle-create inline error (replaces alert), template-404 fallback to empty editor. AI gateway keys set in production (ops). |
| 4.2 | Jun 2026 | AAR Studio plan gate | Closed the direct-nav entitlement bypass: AAR Studio boots behind a fail-open entitlement gate (`entitlement.js` probes `/api/auth/entitlements`; signed-in-but-unentitled orgs see an upgrade screen, signed-out/local-first use proceeds). 13 new node --test tests. |
| 4.3 | Jun 2026 | C3 AI top-up + C4 member invite/activation + limit upgrade UX | C3: one-time Stripe top-up pack credited as `aiBonusSessions`; speech-token gate consumes bonus after monthly allowance; OrganizationPage shows bonus meter + buy button. Limit upgrade UX: `ApiLimitError` on 403+`upgradeRequired`; SignInPage/CreateStationModal/VehicleManagement surface "Upgrade plan" CTA. C4: `Member.authStatus/inviteToken/inviteEmail` in both DB twins + `IDatabase`; `POST /api/members/:id/invite` (admin-only, generates token); public `GET/POST /api/members/activate/:token` (in `memberActivation.ts` mounted before `requireSession`); frontend activation page at `/activate/:token`; invite button on member profile page (admin-only). `api_register.json` → v1.9.0. |
| 4.4 | Jun 2026 | T1 inc 2 — shared domain types | Extracted the core sign-in domain shapes into a single date-generic, declaration-only `shared/domain-types.d.ts` (`Member<TDate = string>` etc.). Backend re-exports specialised with `Date`, frontend with `string`. Type-only `.d.ts` is never emitted/bundled, so install, both `dist` outputs, and CI deploy packaging are untouched (deliberately avoids the T6 npm-workspace conflict). All CI gates green. |
| 4.5 | Jun 2026 | S1 closed | Design-system pass closed: AAR Studio global `prefers-reduced-motion` guard shipped (#587); strict-60px declined by owner (AAR is a desktop facilitator tool); shared report-template sub-item investigated and closed as not-actionable — the three "export paths" use three different mechanisms (CSV / jsPDF+html2canvas / HTML) with no duplicated template to extract. |
| 4.6 | Jun 2026 | AAR P1 (merge UI) | AAR findings board gained a "Possible duplicates" merge-suggestion panel: dedupe now has a two-band model (auto-skip ≥0.72; soft-band [0.5,0.72) surfaced for human merge via `findMergeSuggestions`/`mergeFindings`), with Merge / Keep-both actions and an accessibility pass (role=group/list columns, aria-live status, labelled merge region). 4 new pure tests; AAR suite 89. |
| 4.7 | Jun 2026 | AAR P1 (unit attribution) | AAR findings can be attributed to an attending unit: optional `unit` on the finding model (`createFinding`/`normaliseSession` + `sessionUnitNames`), a unit `<select>` on board quick-add + inline edit, a `chip--unit` on cards, and a conditional **Unit** column in the report's findings register. 4 new tests; AAR suite 93. |
| 4.8 | Jun 2026 | AAR P1 (`?demo` deep link) | `/aar/?demo` loads the bundled example review straight onto the findings board (shareable walkthrough link); `main.js` strips the query after loading, fails open if the example can't be fetched, and reuses the home view's `loadExampleSession()` loader. Only print-stylesheet refinements remain in P1. |
| 4.9 | Jun 2026 | A1 inc 1 (appliance zones) | First slice of the AI maintenance-agent truck model: `ApplianceZone` per-truck spatial entity end-to-end on the backend — types, in-memory DB + Table Storage twin (partition by applianceId) + factory, and admin-gated CRUD routes on the truck-checks router. Walk-around `order` + canonical `zoneCode` slug. 15 tests; `api_register.json` → v1.10.0. Remaining A1: ApplianceEquipment, Appliance/ChecklistItem extensions, zone-vocabulary seed, admin UI. |
| 4.10 | Jun 2026 | A1 inc 2 (appliance equipment) | `ApplianceEquipment` per-truck inventory end-to-end (type, in-memory DB + Table Storage twin + factory, admin-gated CRUD `…/appliances/:id/equipment` + `…/equipment/:id`), with optional `zoneId`, `equipmentCode` slug, and an `active` retire flag (`?includeInactive`). Added a reusable mock-`TableClient` test harness covering both appliance Table twins (~8% → ~92%). 17 new tests; `api_register.json` → v1.11.0. |
| 4.11 | Jun 2026 | A1 inc 3 (checklist links) | `ChecklistItem` gained the zone/equipment link fields (`zoneId`, `equipmentId`, `expectedResponseType`, `unit`, `promptHint`) — additive/optional, round-tripping through both the per-appliance custom overlay and `VehicleType` standard items with no persistence changes (items are spread + stored as JSON). The bridge that lets a checklist item reference a physical zone/equipment piece. 2 round-trip tests; 733 pass. |
| 4.12 | Jun 2026 | A1 inc 4 (appliance agent context) | `Appliance` gained `variant`, `inServiceDate`, `quirksNotes` (free-text brigade knowledge for the agent), wired through `extractApplianceDetails` + `ApplianceDetails` + create/update in both DB twins (in-memory + Table Storage columns). 3 round-trip tests; 736 pass. |
| 4.13 | Jun 2026 | A1 inc 5 (zone-vocabulary seed) | `backend/src/constants/zoneVocabulary.ts` — code-level taxonomy mapping `VehicleType.code` → `ZoneSpec[]` walk-around sequences for Cat 1 tanker / heavy tanker / pumper / bulk water tender / rescue tender / command-support (NSW RFS classes). Zones auto-seed on `POST /appliances` when a `vehicleTypeId` is supplied (non-fatal catch). New admin endpoint `POST /appliances/:id/seed-zones` with idempotent default + `?replace=true`. 18 new tests; 754 pass, all coverage gates green. |
| 4.14 | Jun 2026 | A1 inc 6 (zones & equipment UI) | `ApplianceZonesModal` two-tab admin authoring UI for per-truck zones and equipment, wired into Vehicle Management cards via a "Zones" button. Zones tab: seed-from-vehicle-type bar, ordered zone list, inline edit/delete, add form. Equipment tab: active/retired toggle, inline edit, retire/restore/delete, zone picker, add form. `ApplianceZoneSide`/`ApplianceZone`/`ApplianceEquipment` types in `frontend/src/types/index.ts`; 9 API methods in `frontend/src/services/api.ts`. 11 new Vitest tests; 464 frontend tests pass. A1 data-model track feature-complete. |
| 4.15 | Jun 2026 | A3 inc 1 (voice-agent backend plumbing) | D3 decisions resolved (backend proxy audio, Azure OpenAI, same App Service). Backend plumbing for voice-agent sessions: `AgentSession`/`AgentTurn` types (+ `CheckRun.source`/`agentSessionId`); in-memory + Table Storage twins; factory; WS stub at `/ws/agent-check` (JWT auth, 30-s ping keepalive, session lifecycle); REST `GET /api/agent-sessions/:id[/turns]` behind `requireFeature('aiEnabled')`. 20 new tests; 774 backend tests pass. |
| 4.16 | Jul 2026 | A3 inc 2 (voice-agent tool loop, text mode) | The agent runs a full check over `/ws/agent-check` in text mode: five design-contract tools (`agentTools.ts`), Azure OpenAI function-calling loop with full AgentTurn transcript (`agentLoop.ts`), shared `effectiveChecklist.ts` resolver, `createCheckRun` provenance (`runDetails` in both twins), gateway `tools` passthrough, `user-text`/`agent-text`/`end-session` WS frames, abort-on-disconnect semantics. 21 new tests; 795 backend tests pass. |
| 4.17 | Jul 2026 | A3 inc 3 (voice layer: Azure Speech + PWA push-to-talk) | Server-side Azure Speech STT/TTS (`agentSpeech.ts`, REST short-audio + push-to-talk per the backend-proxy D3 decision), WS audio frames (`audio-start`/PCM/`audio-end` → `transcript` + `agent-text` + binary MP3 reply), and the PWA voice UI (`/truckcheck/voice/:applianceId`, `FeatureRoute aiEnabled`, hold-to-talk + typed fallback) with a hub entry button. 35 new tests incl. the first WS integration suite; backend 817 + frontend 500 pass. |
| 4.18 | Jul 2026 | A3 code-review hardening (F1–F16) | Closed all 16 findings from the pre-rollout code review: WS tenancy/entitlement scoping (F1–F3), WS rate/size/turn/connection caps + Origin check (F4–F6), dev WS origin fix (F7), iPad audio capture/playback fixes (F8), WS reconnect-with-backoff + session resume + pong-timeout (F9), ETag-conditional `AgentSession.status` updates (F10), frontend `onBusy` recovery (F11), join-active-CheckRun instead of duplicating (F12), transcript/buffer/exception-handling fixes (F13–F15), Pointer Capture on hold-to-talk (F16), plus registry docs + minor conventions cleanup. Every fix has a regression test; backend 855 pass/1 skip, frontend 513 pass/16 skip, AAR Studio 93 pass. |
